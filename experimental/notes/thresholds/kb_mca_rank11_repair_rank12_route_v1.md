# KoalaBear rank-eleven repair and rank-twelve dense-core route

Status: proved rank-eleven payment and rank-twelve route cut; zero active-v4
ledger movement.

The uniform rank-one weighted-line cap is

```text
4,070,947
```

over every residual dimension `1 <= j <= 1,048,576`.  The corrected
rank-eleven descent forces `5,201,865`, leaving slack `1,130,918`.

The rank-twelve barrier descent forces

```text
8,681,730 slopes
direction rank at most two
ambient dimension at least 4,280.
```

At the full `K=2` endpoint, the `T=1922` low family has `8,550,040`
slopes, between 9 and 15 pair types, and at least three deficiency-one pair
types.  The current independent-capacity relaxation is `8,829,951`, leaving
the exact missing cross-pair saving `279,912`.

The next theorem must couple the ratio fibers of different fixed pairs.
