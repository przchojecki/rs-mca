# KoalaBear v4 K3 source-bound compiler route cut v1

## Verdict

**Success condition B.** The requested complete source-bound K3 ledger cannot
be constructed from the current proved hypotheses. The scope of this packet
is only the declared active residual slice

\[
Z_{BC}^{(m=2,r=4)}.
\]

Neither a source-bound membership predicate for that slice nor an exhaustive
reduction from all of `Z_BC` to it is proved here. Within that declared slice,
the first missing bridge is earlier than any of the eleven route eliminations:

> an active first-match balanced-core bad affine slope must admit a total
> canonical selector into supplied actual endpoint records and actual
> bidegree `(4,4)` components, together with reconstruction/projection maps
> preserving the received line, support, slope, owner, and add-back position
> and an exact finite projection-fiber bound.

No such selector/reconstruction theorem exists in the current source. A
two-sided inverse would be a sufficient, stronger formulation, but is not
required if a total canonical selector and exact projection-fiber bound are
proved. The source-pencil compiler explicitly works **per supplied actual
endpoint record** and
distinguishes its endpoint parameter line from the evaluation carrier. The
global K3 audit is therefore YELLOW: the orientation geometry is useful, but
it is not yet source-bound. The narrower proposed `O0b -> O0a` transport is
RED within the frozen natural map class described below. No claim is made
against arbitrary orientation-changing algebraic maps.

This is a rigorous route cut, not a numerical heuristic and not a claim that
the missing source-bound theorem is false.

## Frozen active object

The active row is KoalaBear MCA at agreement `1116048`:

```text
p                  = 2130706433
extension degree   = 6
n                  = 2097152
k                  = 1048576
B*                 = 274980728111395087
architecture       = GRANDE_FINALE_V4_KB_MCA_TANGENT_SOURCE_ADAPTER_V1
partition digest   = 4fade91abc408264989babcff6f8f9bbd80bcec52545a5db15ac376bf17d88fc
owner              = ACTIVE_V4_BALANCED_CORE, priority 2
unit               = distinct bad finite slopes per received line
quantifier         = uniform over all admissible received lines
```

For the bad-slope set `Z`, the active first-match chronology is

\[
\begin{aligned}
Z_{\rm paid}&=Z\cap T, &R_1&=Z\setminus Z_{\rm paid},\\
Z_Q&=R_1\cap Q, &R_2&=R_1\setminus Z_Q,\\
Z_{BC}&=R_2\cap BC, &Z_{\rm new}&=R_2\setminus Z_{BC}.
\end{aligned}
\]

The K3 source-bound compiler considered here would have domain only the
declared slice `Z_BC^(m=2,r=4)`. Its membership predicate and an exhaustive
reduction from all `Z_BC` remain open. By contrast, the proved source-pencil
identities classify a **supplied actual record**:

\[
V_{\rm act}\in\operatorname{Sym}^{60/m}(W),\qquad
A_S\in W,\qquad A_R^5\in W.
\]

Those equations do not construct the required selector from the declared
slice to that record, much less a chronology-preserving reconstruction with
an exact projection-fiber bound. Similar parameter values and workboard names
are not a composition theorem.

## Imported raw dependency

The raw `433-1b -> O0a` workboard exclusion at public-DAG commit
`8df0903391a228eed6e24398fca9d40d72d546cf` was replayed. It proves:

```text
15 role cells
1575 raw labels
25200 signed principal systems
0 surviving raw systems
ledger movement 0
```

For each of the two raw-zero routes in the table below, the
surviving-raw-system count is exactly zero, while its distinct-affine-slope
payment is `null`. The zero may be
transported only after the active source-bound selector/projection theorem is
proved. The raw certificate itself correctly disclaims exhaustive
balanced-core routing and raw-label-to-slope conversion.

## Exact positive route table

The common and outside defect/loop laws reproduce exactly thirteen retained
routes. Two are proved raw-zero (zero surviving raw systems, but `null`
distinct-slope payment):

```text
433-1a -> O0b
433-1b -> O0a
```

The eleven remaining routes are unchanged:

```text
442-0a -> O0b, O1b, O1d
442-1b -> O0a, O0b
433-0  -> O0a, O0b, O1b, O1c, O1d
433-1b -> O0b
```

None has a literal distinct-affine-slope integer. The generic one-pencil cap
does not pay a union of undeclared pencils, and no exact relevant-line count
has been instantiated on these routes.

## The attempted `433-1b -> O0b` transport fails exactly

The obstruction below is deliberately scoped to the declared natural map
class generated by uniform target-vertex gauges, interchange of the two
parallel BC records, swapping the `B/C` roles, source-root sign/deck
relabelings declared by the pinned atlases, and typed outside-role
permutations preserving the displayed incidence data. It does **not** rule
out arbitrary orientation-changing algebraic maps. Any broader transport
must first be stated with its exact action on the full record and then proved
separately.

### Repeated BC

On the guarded open `bc != 0`, define the ordered parallel-product ratio

\[
R_{BC}=\frac{p_{BC,2}}{p_{BC,1}}.
\]

The proved product records give

\[
(-1,b,c,bc,-bc)\quad\hbox{for }O0a,
\]

and

\[
(-1,b,c,\sigma bc,\sigma bc)\quad\hbox{for repeated-BC }O0b.
\]

Hence

\[
R_{BC}(O0a)=-1,\qquad R_{BC}(O0b)=+1.
\]

Target vertex gauge multiplies both products by the same factor. Swapping
`B/C` or the duplicate roles merely permutes them. Source-root signs and
outside-label reindexing do not alter the target products. Since the deployed
characteristic is odd and `bc` is guarded nonzero, the two values differ.
Thus no repeated-BC common-system bijection in the declared natural map class
can preserve the product equations.

### Split BC

The **common** split compiler transfers exactly, but the full outside system
does not. The permutation-invariant unsigned incidence signature

\[
\Xi=(\operatorname{sort}(r_i),\operatorname{sort}(m_{ij}))
\]

is

\[
\Xi(O0a)=((0,0,2),(1,1,3)),\qquad
\Xi(O0b)=((0,1,1),(1,2,2)).
\]

All six typed permutations of the outside roles were replayed exactly and
none is an isomorphism. Concretely, the tempting `D/E` map sends the `BE`
incidence to an absent `BD` incidence; a sign gauge cannot create an edge.

This forbids importing the O0a `105 -> 36` quotient or its outside closure
into O0b through the declared natural map class. It neither rules out a newly
specified orientation-changing algebraic transport nor proves the O0b route
nonempty; native O0b algebra may still close or pay it.

## Exact local data and conditional workload

### Orientation side

For each **supplied actual** record, the transverse source compiler has
`32099` partitions. This number is bound directly to
`background/nodes/rate_half_kb_source_pencil_rank_transverse_compiler/statement.md`
at public-DAG commit `48a7de3c2d0d092b1899b1bb18d62bb4bf8861ce`
(content SHA-256
`0c1756ccf94e7b4503bf7b9aa5a39cebf16abaf1a200ec9496dd8bcf6b6f664f`).
It is per supplied record, not a global bad-slope census.

The exact fixed-point-free involution replay on twelve source labels gives:

| diagonal row `(a,b,c)` | labeled involutions |
|---|---:|
| `(2,0,2)` | 1350 |
| `(1,1,2)` | 2700 |
| `(1,0,4)` | 3600 |
| `(0,1,4)` | 1800 |
| `(0,0,6)` | 720 |

There are `10395` total involutions, of which `225` are
partition-preserving and deleted. Only the saturated `(1,1,2)` source-line
workboard is closed. The rows `(1,0,4)`, `(0,1,4)`, `(0,0,6)` and exceptional
orbit `KBDM-10` have no declared closed image. The independent transverse
type `(m,r,delta)=(2,8,1)` also lies outside the proposed three orientations.

The source-cover branch has four live diagonal rows and two declared
genus/passport forms per row, hence eight unresolved **candidate
row/passport combinations**. This is not a terminal-cell census or a count of
realized source-cover components: no source-cover workboard object exists, so
`U_sourcecover` is undefined.

### Native O0b side

The latest proved owner partition remains

```text
split rank five          360 rows   37800 raw labels
repeated cells 1/2        16 rows    1680 raw labels
repeated cells 11/14      32 rows    3360 raw labels
total                    408 rows   42840 raw labels
```

The exact outside-label orbit replay gives:

```text
O0b d -> -d                 57 orbits: 1:9, 2:48
O0a universal               36 orbits: 1:3, 2:15, 4:18
O0b identical pair          60 orbits: 1:15, 2:45
O0b S0 role map             56 orbits: 1:7, 2:49
O0b S0 combined maps        32 orbits: 1:3, 2:7, 4:22
```

The current split-block arithmetic gives the conditional candidate
abstract-label-orbit workload `11304`:

\[
0+2\cdot15\cdot4\cdot60
+2\cdot6\cdot4\cdot57
+2\cdot3\cdot4\cdot57=11304.
\]

This number is **not** a census, elimination, representative count, or
payment. The pinned `label_orbits.py` explicitly disclaims census and
elimination: its terms are conditional abstract-label-orbit bookkeeping, and
the proposed widened-scope quotient/transport inputs remain unproved. In
particular, `11304` is neither a raw-system survivor count nor a count of
distinct slopes.

## PR #1155 reconciliation

PR #1155 commit `1cf13bb4058da19c5108bf79472394a598217bca` was replayed. Its
sixteen mutation tests pass and its exact guarded necessary point survives.
It validly cuts the proposed `433-1a` signed-pair guard transplant: the
cross-remainder does not vanish and a residual quadratic cover remains.

Classification:

```text
VALID_GUARD_TRANSPLANT_ROUTE_CUT_REGRESSION_DEPENDENCY
```

It is neither superseded by PR #1152 nor a closure/payment. As an operational
author audit, the original dirty worktree and all uncommitted cover/transport
files were left untouched. That worktree classification is deliberately
absent from the mathematical certificate: local paths and working-tree state
are not proof inputs. The local `cover-bound`, `transport-route-cut`, and
large-characteristic normalizer artifacts are preserved for regression use;
the present packet neither refutes nor deletes them. Further cell-11
refinement is not an active-ledger attack while the earlier source and unit
bridges remain open.

## Current upstream overlap audit

The open-PR board was refreshed after the packet stabilized. This section is
a non-load-bearing temporal snapshot: PR state can change without changing
the proof. The only source-bound overlap claim made by the certificate is
that the pinned dependency set listed in its source bindings contains no
active-slice selector/reconstruction/projection theorem.

- PR #1152 at `ed4877cce5f2` is the exact stacked dependency and supplies the
  imported raw-zero route plus the local O0b boundary cuts.
- PRs #1153--#1155 are local independent replays, compact-tower work, and the
  guard-transplant cut. They do not construct the active source-bound map.
- PR #1130 compiles the endpoint source-pencil fibers, but explicitly
  distinguishes the endpoint parameter line from the evaluation carrier and
  disclaims the same-record parameter-to-carrier bridge.
- Draft PR #1132 classifies supplied actual order-two components and their
  coordinate/diagonal interfaces. It does not map active `Z_BC` slopes to
  those components or print a slope payment.
- New draft PR #1156 concerns denominator-root and coordinate-clone branches
  of a different MCA exception router. Its source-contract warning—that an
  owner-line incidence is not automatically a selected-support root—supports
  the same fail-closed discipline but does not pay or duplicate K3.
- Closed PR #1119 was an abstract source-map-class syntax/compiler. Its own
  nonclaims exclude a semantically validated exhaustive selector, so it is
  not the missing active-to-actual theorem.

At the time of the snapshot, no inspected open PR defined `U_K3_allocation`,
supplied the source-cover terminal census, or proved the required
received-line/support/slope/owner/chronology-preserving selector and exact
projection-fiber theorem. That status observation is not a proof dependency.
The durable conclusion is narrower: the pinned dependency set does not
contain the bridge. This packet is therefore a new composition audit and
maximal route cut, not another cell-11-only closure PR.

## Exact ledger consequence

The tangent manifest declares the following arithmetic:

```text
B*                         = 274980728111395087
U_paid (manifest-declared) =             981104
joint Q/BC/new reserve     = 274980728110413983
```

This packet does **not** transitively revalidate that payment. The tangent
manifest and row expect the `experimental/grande_finale.tex` Git blob

```text
8a5d9791900ca9eed773feba146b92ad296704ce
```

whereas PR #1152 contains

```text
6b21d6ea937a8a9f85fc7ade6032d73efd4c7222.
```

The same row manifest records `agents.md` steering blob
`30d8b9f1b4caa3c7504fe3d24fc7ce8da84de434`, whereas exact PR #1152 has
`75a01e4b6cfc17c34112a47bd51294519ebc687c`. The tangent verifier reports
that second drift as a note, not its proof-source gate; the packet records it
separately and does not use steering prose as mathematical evidence.

Accordingly both
`verify_kb_mca_v4_tangent_source_adapter_v1.py --check` and its optimized
`python3 -O` replay fail on the `active_v4` source hash. Thus `981104` and
the subtraction producing `274980728110413983` are manifest-declared exact
integers, but they are not source-revalidated/banked by this packet. The
joint reserve is in any event not a K3 allocation. Consequently the honest
output is:

```text
U_remaining       = null
U_positive        = null
U_sourcecover     = null
U_K3              = null
U_K3_allocation   = null
signed slack      = null
ledger movement   = 0
```

Assigning zero or substituting the joint reserve would change units and
chronology. The packet's mutation suite explicitly rejects both operations,
and it rejects promotion of the stale tangent source gate.

## Tool and proof status

- Python big integers reconstruct the literal ledger, route atlas, canonical
  JSON, source hashes, and hostile mutations. Normal and optimized packet
  replay are the required passing checks; the two separate tangent-adapter
  checks fail as expected on the stale `active_v4` source pin and are not
  counted as packet passes.
- Sage independently replays the deployed field, thirteen routes, all
  `10395` involutions, the `105`-label orbit tables, incidence matrices, and
  totals.
- FLINT independently replays the odd-characteristic invariant, exact large
  integers, and all six incidence-matrix permutations.
- Wolfram independently replays the integer totals, orbit-count identities,
  and matrix non-isomorphism.
- Singular/Macaulay2 are not invoked because the first obstruction is a
  missing source-bound map, not a fixed residual ideal.
- Exa/TheoremSearch are not invoked: after freezing the gap, it is a
  project-specific carrier/owner adapter obligation rather than a missing
  general syzygy or curve theorem.
- Lean is intentionally deferred until a local source-bound theorem exists.

Fresh, non-generating proof and certificate/custody reviewers independently
classified this packet GREEN and PR-ready for **success condition B** after
the repaired replay. That verdict is not GREEN for K3, the KoalaBear row, or
an all-`Z_BC` theorem; those conclusions remain expressly open above.

Layer cake, dyadic summability, moments, Markov, and Chebyshev do not occur in
this finite exact route cut.

## Maximal next theorem

The next useful theorem is not another cell-11 eliminant. It is the exact
active-to-actual compiler:

> First define and prove the source-bound membership predicate for
> `Z_BC^(m=2,r=4)` and its claimed scope within `Z_BC`. For every `z` in that
> declared slice, construct a total canonical selector into supplied actual
> endpoint records and actual components, with reconstruction/projection maps
> preserving the received line, complete witness support, affine slope,
> active owner, and add-back chronology. Prove the selected image is
> exhaustive for the declared slice and state an exact finite projection
> fiber bound. A two-sided inverse is a sufficient stronger form, not a
> prerequisite if these data are proved directly.

Only then can the already-proved orientation trichotomy, raw zero routes,
native O0b reductions, and future source-cover terminals be summed in slope
units. A later theorem is still needed to reduce all `Z_BC` to this slice or
route its complement. If the local selector theorem is false, its required
falsifier is an explicit declared-slice slope with no selected component,
uncontrolled incompatible components, a projection fiber exceeding the
claimed bound, or failed reconstruction of the declared record.
