---
workboard_item: K1
row: KoalaBear MCA at 2^-128
object: MCA
target_epsilon: 2^-128
agreement: 1116048
B_star: 274980728111395087
direct_statement: Rebuilding the full-outside rank-nine selector after the six active source owners and replaying the carrier-sensitive one-cut compiler at the exact active reserve closes two slack ranges and the coarse tail with zero additional charge.
architecture: GRANDE_FINALE_V4_KB_MCA_TANGENT_DEEP_SOURCE_RATIONAL_C5_BASE_TWIST_FROBENIUS_9208_ADAPTER_V1
atom_or_cell: ACTIVE_FULL_OUTSIDE_CARRIER_INCIDENCE_BRANCH_COMPILER
quantifier: Per received line, fixed translated source, and rebuilt complete selector
projection_and_unit: Distinct bad finite slopes per received line
claimed_bound: zero additional charge on r=9209..67466 and r=236113..913631 in the declared full-outside rank-two branch
status: PROVED
impact: ZERO_CHARGE_BRANCH_CLOSURE
falsifier: Failure of the rebuilt selector contracts, a scan endpoint different from the printed intervals at the active reserve, or use of selector data from before the six active deletions.
replay: python3 experimental/scripts/verify_kb_mca_v4_active_carrier_incidence_replay_v1.py --check
---

# KoalaBear active carrier-incidence replay

**PROVED ACTIVE BRANCH COMPILER / ZERO ADDITIONAL CHARGE / ROW OPEN.**

This packet replays the proved full-outside carrier-incidence compiler on the
exact six-owner active residual. It does not import the predecessor selector
or its degree-195 ledger.

## 1. Active input

The active source-bound partition has

\[
U_{\rm paid}=19{,}625{,}940{,}372{,}935
\]

and exact remaining reserve

\[
B_{\rm rem}=274{,}961{,}102{,}171{,}022{,}152.
\tag{1.1}
\]

The degree-\(9{,}208\) source-Frobenius owner is followed by mandatory
selector restart. Consequently, every qualifying outgoing full-outside
coefficient-rank-two rich record has

\[
r=s-t-1\ge9{,}209.
\tag{1.2}
\]

The translated source pair and domain order remain fixed. Every carrier,
graph line, affine-rank minimizer, basis atlas, deficit histogram, and
selected support is recomputed on the new residual.

## 2. Reused exact inequalities

For fixed slack \(r\), the source size and maximal carrier are

\[
s=t+r+1,\qquad N_V^{\max}=n-s.
\]

The source-rational restart gives

\[
x_L\ge x_0(r)
=\left\lceil\frac{t-r+1}{2}\right\rceil.
\]

For the low-deficit cutoff \(D=18{,}014\), every contributing graph line has

\[
J_L\le J_*(r)=
\begin{cases}
1+\lfloor j/x_0(r)\rfloor,&x_0(r)\ge1,\\
j+1,&x_0(r)\le0.
\end{cases}
\]

The fixed-basis atlas then gives

\[
H_D\le
\left\lfloor
\frac{J_*(r)\binom{N_V^{\max}}8}
{\binom{t+8}8}
\right\rfloor.
\tag{2.1}
\]

The exact two-bin one-cut compiler is recomputed with the active
\(B_{\rm rem}\) in (1.1). No legacy threshold or remaining-budget integer is
copied.

## 3. Exact active scan

The verifier scans every integer

\[
9{,}209\le r\le913{,}631
\]

and obtains exactly:

| slack \(r\) | active terminal |
|---:|:---|
| \(9{,}209\ldots67{,}466\) | `PAID_ACTIVE_FULL_OUTSIDE_RICH_PENCIL_CARRIER_ONE_CUT` |
| \(67{,}467\ldots236{,}112\) | `UNPAID_ACTIVE_FULL_OUTSIDE_LOW_X_DETERMINANT_PACKING_SLACK_67467_TO_236112` |
| \(236{,}113\ldots330{,}348\) | `PAID_ACTIVE_FULL_OUTSIDE_RICH_PENCIL_CARRIER_ONE_CUT` |
| \(330{,}349\ldots913{,}631\) | `PAID_ACTIVE_FULL_OUTSIDE_M2B_COARSE_CARRIER` |

The terminal counts are:

```text
one-cut paid       152,494
coarse paid        583,283
open               168,646
total scanned      904,423
```

There is no failed one-cut threshold.

The first transition remains at \(67{,}466/67{,}467\), where the exact
integer line cap jumps as \(x_0\) drops from four to three. The active reserve
moves the second transition from the legacy \(236{,}097/236{,}098\) boundary
to

\[
236{,}112/236{,}113.
\]

At the two sides, the exact tail margins are

\[
-416{,}764{,}420{,}032
\quad\text{and}\quad
1{,}071{,}306{,}706{,}736.
\]

The coarse transition moves from \(330{,}335/330{,}336\) to

\[
330{,}348/330{,}349.
\]

## 4. Meaning of zero additional charge

For a fixed received pair, \(r\) is one fixed source integer, not a profile
over which charges are summed. On a paid interval, the rebuilt selector
compiler proves that the complete counted full-outside branch fits inside
the already available reserve \(B_{\rm rem}\). It therefore closes that
branch without adding another owner cap to \(U_{\rm paid}\).

This is not an assertion that several separate branches may each spend
\(B_{\rm rem}\) and then be summed. The terminal is an alternative branch
conclusion in the fixed-pair proof.

## 5. Exact remaining carrier target

The only scalar gap left by this active replay is

\[
\boxed{67{,}467\le r\le236{,}112.}
\tag{5.1}
\]

For every integer in (5.1), the verifier also constructs an abstract
disjoint-basis packing satisfying every scalar inequality consumed by the
compiler and exceeding the active reserve by one. Thus improving only the
same scalar line-cap/basis-cap interface cannot close the gap. The next
theorem must use deployed determinant packing, correlation between graph
lines, or another exact source owner.

The packet does not construct a Reed--Solomon counterexample; the abstract
packing is a route cut only.

## 6. Proof authority and nonclaims

The source theorem is:

```text
experimental/notes/m1/
m1_kb_rank9_full_outside_carrier_incidence_splice_v1.md
```

The active predecessor is:

```text
experimental/notes/frontier-adjacent/
kb_mca_v4_c5_twist_frobenius9208_adapter_v1.md
```

The verifier binds both, recomputes all \(904{,}423\) slack rows at the
active reserve, hashes the scan and route-cut witnesses, checks boundary
margins, and rejects semantic mutations.

This packet does not pay the intermediate gap, non-full-outside source load,
Q, balanced core, or the final complement. It does not import old selector
data and does not close the KoalaBear row.

# PROVED
