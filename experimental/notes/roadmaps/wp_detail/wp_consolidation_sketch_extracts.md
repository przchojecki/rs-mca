# Consolidation (L3): WP-0.1, WP-2.1, WP-2.2, WP-2.4, WP-5.1, WP-7.1

- **Status:** consolidation — these six packages were substantially
  detailed inside the proof-sketch tree (organized by mathematics); this
  note gives each its per-package L3 form BY CITATION plus the
  operational glue only. No new mathematics, no new numbers (every cited
  figure was machine-verified in its source note).
- **Executability:** AGENT for all six (WP-5.1's core is also a
  bottom-up hand-off).

## WP-0.1 — object reconciliation [gate]

```text
statement   the 9-axis ledger of s5_s0 §4 is NORMATIVE: axes 3/5/7
            verified, 6 dictionary-done (s4), 1/2/4 open definitional
            audits, 8/9 rules lookups (8 = generating, 9 = dither,
            provisionally resolved by the wp0_2 live fetch).
route       axes 1/2/4: quote-level audits against the ABF text using
            wp0_2's fetch/hash protocol; extend the existing step-1
            reconciliation + M0 freeze (assets, on main) — never a
            fresh document; axis 4 is now SHARP: "is ABF's Delta_S the
            degenerate-pencil predicate?" (s4 §2).
acceptance  every axis EQUAL / BRIDGED(loss printed) / OPEN; zero OPEN
            before any prize-facing claim (the wp7_4 v-gates consume
            this directly); deliverable object_reconciliation_abf.md.
failure     adverse axis -> new ledger column + mechanical reprint
            (s5_s0 F4); constants shift, structure does not.
```

## WP-2.1 — front alpha [replay-gated]

```text
statement   alpha-strong/weak gcd targets as in the r1 inventory;
            evidential meaning fixed by s3a §3 (P1a / P1b / P1c).
route       AFTER wp0_3 replay: counterexample-first full-grid scan on
            the RE-DERIVED definitions; then the ladder (generalized-
            spectrum reformulation -> resultant route -> moment-run
            rigidity -> char-17 hygiene), stopping at the first
            resolution either way.
acceptance  a scan artifact classifying the outcome P1a (gcd = 1
            everywhere) / P1b (collisions, all paid — each run against
            the tangent/quotient ledgers) / P1c (unpaid collision:
            candidate_new_obstruction with minimal reproduction).
failure     P1c -> s3a F1 (new ledger in the easiest regime, corridor
            moves down, high information); scan infeasible at some
            (m, r) -> shrink grid, report coverage, never sample
            silently (no-silent-caps rule).
```

## WP-2.2 — front beta [replay-gated]

```text
statement   beta1/beta2/beta3 trichotomy (compressed 6x6 table within
            budget / all paid / counted Hankel-realized sharpness);
            geometric prior fixed by s3a §4 (ambient-vs-Hankel codim
            ~29068: realizability must be structure-forced).
route       realizability search FIRST (cheapest decisive step); if
            unrealizable, isolate and prove the Hankel obstruction; if
            realizable, run the paid-ledger witnesses and count.
acceptance  a realizable-or-not artifact; if realizable, the counted
            stratum with its payment classification (P-beta outcome).
failure     unpaid realizable sharpness -> s3a F2: direction-rank
            methods insufficient in-window; wp2_5 charts become
            mandatory for the window theorem.
```

## WP-2.4 — the M4 deduped table [assembly input]

```text
statement   the per-A table with columns and semantics exactly as
            s2 §5 (Paid(A) spec): tangent, quotient zones a/b/c with
            zone-(b) as INTERVALS, extension, aperiodic columns,
            deduped total vs B*.
route       generator script consuming the wp3_2 descriptor + ledger
            artifacts; dedup = the wp2_3 tree order (first-match),
            conditional cells while alpha/beta open (wp0_2-style
            honesty in table form).
acceptance  deterministic regeneration (same inputs -> same table);
            WP-0.4 checker H2 (recompute) + H5 (dedup) green on the
            emitted table; Lean G3 mirrors the arithmetic (wp7_3).
failure     dedup mismatch anywhere -> the tree order is normative;
            regenerate (one convention, never two).
```

## WP-5.1 — L1 image-fiber bound [the list-side core]

```text
statement   #ImgFib_U(a) <= n^B above eta >= (1+eps) tau*(rho, q_D)
            with the quotient profile budgeted — image fibers, never
            raw (s7 §4); the concrete open sub-battle is the full-petal
            sunflower growth (witnesses at d-ell = 2, 5 on main;
            exact-rank shortcut route-cut).
route       (i) the Conjecture-F dictionary: petal extras are
            structured plane sections of the divisor set — attack with
            the same SPI/XR machinery as the MCA side (fourth
            appearance of the one collision family); (ii) the averaged
            version via Johnson-scheme second moments (s3b_iii_2 §5,
            plausibly provable now); (iii) growth bound or mixed-petal
            amplification as the worst-case statement. Bottom-up
            hand-off shaped.
acceptance  either a proved growth bound consumable by L2 (wp5_2's
            budgets then bind), or the petal wall frozen per standing
            order 8 with the existing witnesses as toy reproduction.
failure     fails at the reserve -> both challenges' safe sides move
            (s7 F3); S9 posture — determine the enlarged ledger.
```

## WP-7.1 — the assembly compiler [executable theorem]

```text
statement   the I/O contract of s8_s9 §1 is NORMATIVE (gates both,
            generating check, Paid(A) interval cells, four verdict
            classes with H6 printed, refusal rule; list side with
            q_chal printed).
route       implement over the wp3_2 descriptor + the wp2_4 table +
            packet refs; refusal wired to the S0 ledger and the
            conjectural-dependency scan (wp0_4 H6).
acceptance  three regressions: (1) pinned row -> emits the 506/507 pin
            UNCONDITIONALLY (envelope path); (2) Row A -> corridor
            cells UNKNOWN / SAFE(conditional, H6 printed) exactly as
            s8_s9 §2 predicts; (3) refusal fires on a prize-facing
            request with an OPEN S0 axis (negative control).
failure     any unconditional verdict traced to a conjectural ledger =
            bug class zero — a negative-control packet exists for it
            (wp0_4 suite) and MUST stay red.
```

## Coverage note

With this consolidation, every work package WP-0.1 through WP-7.4 of the
r2 roadmap now has an L3 detail note in `wp_detail/` (13 files including
the wp2_6 exemplar). The remaining loop turn is the integration pass:
cross-link the full map into the r2 root and audit the DONE-definition
per package.
