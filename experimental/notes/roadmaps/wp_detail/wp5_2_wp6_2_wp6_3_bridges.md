# WP-5.2 + WP-6.2 + WP-6.3 detail (L3): interleaved constants, LD statements, the bridge ledger

- **Status:** operational detail over PROVED-cited material (largely my
  x1/l2 corpus on main); per-m budgets machine-checked this turn.
- **Executability:** AGENT throughout.
- **Parents:** r2 WP-5.2/6.2/6.3; consumes s7 (list side), s4
  (dictionary), wp0_2 (the m-quantifier finding), wp3_2 (descriptor).

## 1. WP-5.2: interleaved constants — the m-sweep sharpens everything

Assets on main [PROVED-cited]: codegree Theorems A/B/C (L1-free);
`BadVec = Deep_alpha^mu`; the a-regular collapse (interleaved = base,
exponent exactly 1); the `K_{m,m}` clique cap `n >= k + m^2(a-k)`; the
conditional protocol budget (§2.8 of the x1 note).

**The per-m budget table (verified, n = 2^40, log2 q - 128 = 128):**

```text
m      worst-case (e = m)   a-regular (e = 1)
2      B <= 1.60            B <= 3.20
3      B <= 1.07            B <= 3.20
4      B <= 0.80            B <= 3.20
16     B <= 0.20            B <= 3.20
under conj:B-strength L1 (base exponent 1): the worst-case route covers
m <= 3 ONLY; for m >= 4 in the for-all-m reading (wp0_2 axis d), the
a-regular collapse or a better reduction is REQUIRED, not optional.
```

**Remaining mathematical target (the package's one open piece):** reduce
general pairs to the a-regular case, or bound the irregular stratum —
my §2.4 worst-case theorem gives exponent 1 UNDER a-regularity; what is
missing is forcing it. Failure branch: unforceable -> partial for-all
coverage (m <= 3) and the m-quantifier resolution becomes decisive; if
the official reading is exists-m or fixed m = 2, the worst-case route
already suffices. **Acceptance test:** the budget table generated by the
wp3_2 descriptor (no hand constants) + regression against the §2.8
numbers; the a-regularity question stated as a named lemma target for
the bottom-up lane.

## 2. WP-6.2: line-decoding statements without denominator drift

Assets [PROVED-cited]: `m2_line_decoding_mca_bridge.md` (main); my §2.7
(on the simple-pole family, LD = Bad_MCA = Bad_CA = Deep_alpha as SLOPE
SETS — with the caveat, caught and fixed in #101, that one slope can
carry several closing codewords: multiplicity is a list-size, not a
slope-count).

**Deliverable:** for each pinned threshold, the LD-language restatement
with the SAME denominator (no drift — the M2 lane's one rule), the
multiplicity caveat printed, and the endpoint conventions carried
verbatim from the S0 table. **Acceptance test:** a round-trip verifier —
MCA statement -> LD statement -> back — on the 506/507 pin, checking the
gate and endpoints survive both directions unchanged. **Failure branch:**
the official LD object diverges at an S0 axis -> the LD statement
conditions on that axis exactly as the submission note does (wp1_1 §4).

## 3. WP-6.3: the bridge ledger — no silent crossings

**Deliverable:** `experimental/notes/audits/bridge_ledger.md` — one row
per bridge used anywhere in the submission chain:

```text
columns: bridge (list<->CA / CA<->MCA / MCA<->LD / base<->interleaved /
         extension-slice / curve) | direction | radius loss | field loss
         | sqrt loss | multiplicity behavior | support convention |
         sampler | proved where (file) | used where (WP / dossier claim)
```

Rows already known from the corpus: the deep-point identity (lossless on
slope sets, multiplicity caveat); the codegree reduction (agreements
`a` and `2a-k`, no field loss, exponent bookkeeping per §1); the
extension slice (mu = e coupling, denominators per S6); the
tangent-fiber conversion (Conjecture F pricing). **Acceptance test:** a
checker that scans dossier/submission text for bridge keywords and
fails on any use without a ledger row — the executable form of "no
bridge may be used silently." **Failure branch:** a bridge with unknown
loss -> every claim it supports demotes to conditional until the row is
filled; the ledger makes that demotion mechanical.

## 4. What this buys

The list-to-protocol chain gets its constants generated rather than
quoted, the one open reduction (a-regularity) is priced per-m and named
for hand-off, LD statements inherit the audit discipline instead of
introducing a second convention, and the bridge ledger turns standing
order 9's slogan into a checker rule.
