# The kernel basis of the finite prize: seven reds, three kernels
# (roadmap packet; no theorems asserted beyond the cited record)

- **Status:** ROADMAP / AUDIT (each cell backed by a replayed
  verdict at the fork; nothing here changes any node status).
- **Track:** the FINITE prize — prepared for the post-asymptotic
  pivot. Full program record (charter, per-red verdict notes,
  verifiers, catch ledger):
  https://github.com/AllenGrahamHart/rs-mca-prize-dag
  (notes/kernel_basis/T_BASIS_ASSEMBLY.md and siblings).

## The result of the reduction program (2026-07-10)

Every remaining red node of the finite-prize critical surface
carries either a machine-verified conditional-close or a replayed
verdict naming its missing kernel. The minimal kernel basis has
THREE members:

K2* THE ENLARGED RIGIDITY KERNEL — the census/agreement rigidity
    schema (paid taxonomy + primitive remainder <= poly * first
    moment, uniform + joint), with: the census sub-schema as its
    core; the taxonomy extended by the Luroth/net and moment-trade
    classes; extension-type pinned as toral/norm-kernel families;
    a CONSTANT-EXACT strengthening needed exactly where consumers
    have sub-bit slack; and a joint-reserve clause at
    state-dependent junctions (the TR/dli convergence). Serves:
    the F2 dichotomy (via an exact instance identification), the
    F5 spread count, the F3 column budget, dli, and the worst-word
    envelope.
K4* THE PETAL KERNEL — the stabilizer-primitive image-fiber bound
    POOLED with the support-forcing dichotomy (staircase-or-
    primitive in the top defect band). Serves: petal growth. The
    E22 packet family (already integrated here) is its proved
    entry machinery; the quotient-periodic charge is proved
    NECESSARY by the planted lower count.
K5  THE RAZOR-BAND WITNESS KERNEL — priced deficit-side witness
    families through the rate-1/2 band (R(lq), sigma*], uniform in
    q. Serves: rate-half band closure. Structural finding: no
    upper-bound kernel can serve this node (side mismatch); its
    floor's own first-moment model needs per-row formalization.

Minimality is argued structurally (no member closes over the
others); the full arguments and the per-red verdict table are in
the assembly note at the fork.

## The cross-kernel frontier (where ideas are needed)

1. CONSTANT-EXACT clause (ii): two consumers have < 1 bit of
   aggregate slack (the F5 gate at 0.86 bits; the worst-word line
   at 0.9) — unless a conventions re-attribution is ruled (the
   within-column room is 27-60+ bits; see issue #493).
2. The axis-Q small-core classification (rare rank-loss
   configurations must be CLASSIFIED, not counted away) — the
   shared inverse-theorem wall.
3. The joint reserve at nested-conditional junctions (the TR/C2''
   convergence; factorized transfer is measured FALSE at dli's
   junctions — exact correlation ratios in the fork's record).

## Spec items the finite statements will hit first

- The official factorization q = p^k is pinned nowhere; the
  admissible prize-max window is log2 Q in [255.9113, 256) —
  0.089 bits wide (the r2 'prize band underdetermined' note, made
  quantitative).
- Necessary hypotheses for any census statement: see the
  companion packet cap25_finite_census_necessary_hypotheses.md
  (three counterexample families, each with its guard).
