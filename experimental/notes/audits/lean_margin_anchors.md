# Lean margin anchors (core-only)

## Claim
Kernel-checked lower(a0+1)<B_* and gap integers for four deployed rows.

## Build
```
cd experimental/lean/margin_anchors && lake build
```
Lake build completed successfully (Lean 4.14.0, no mathlib).
