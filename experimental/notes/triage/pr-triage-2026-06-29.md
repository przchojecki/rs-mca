# PR Triage 2026-06-29

This pass reviewed PRs #131 through #135 one by one.  The branches were stale
relative to the then-current Paper D v6 and site state, so no branch was merged
directly.  Only new experimental notes, data certificates, and verifiers were
integrated.

## Decisions

| PR | Author | Decision | Reason |
| --- | --- | --- | --- |
| #131 | AllenGrahamHart | Distilled audit only | The boundary-off external-anchor normal form is useful local M1 proof-program material, but the PR also contained a very large generated draft and stale mainline rewrites. |
| #132 | Scott Hughes | Integrated as audit/route-cut | The `a=507` plus-one route cut is a useful guardrail: adjacent `6+1` arithmetic cannot be counted as a seventh finite-slope support-wise event. |
| #133 | Scott Hughes | Integrated as interleaved-list data | The hybrid quotient-residual packet gives a standard interleaved-list lower bound `Lambda_mu(C,326) >= 7`, with route cuts in the high-agreement interval. |
| #134 | Vadim Avdeev | Integrated as finite-slope lower floors | The random simple-pole entropy floor gives very large finite-slope lower bounds for `a=257..260`, including all finite slopes at `a=257,258`. |
| #135 | Vadim Avdeev | Integrated as finite-slope lower floors | The coset-packet construction gives explicit finite-slope lower floors for `a=261..288`, beating the moving-root tangent floor in those rows. |

## Promotion Rules Used

- Finite-slope lower floors were added to experimental notes/data and may be
  displayed on the site as `mca` lower-bound proof records.
- Interleaved-list lower bounds were added only to the `list` track.
- Route cuts and local M1 normal forms were not promoted to leaderboard rows.
- No contributed branch was allowed to overwrite the then-current Paper D v6 files,
  root PDFs, scanner reports, or site fallback structure.

## Authors

- AllenGrahamHart contributed the boundary-off external-anchor normal form and
  primitive quotient-normal M1 target.
- Scott Hughes contributed the `a=507` route-cut audit and interleaved-list
  threshold descent/hybrid certificates.
- Vadim Avdeev contributed the random simple-pole and coset-packet finite-slope
  lower-floor constructions.
