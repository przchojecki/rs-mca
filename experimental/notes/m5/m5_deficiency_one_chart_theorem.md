# M5 Deficiency-One Chart Theorem

- **Status:** PROVED-LOCAL / EXPERIMENTAL.
- **Lane:** M5 underdetermined buckets, first boundary.
- **Verifier:** `experimental/scripts/verify_f17_32_m5_underdetermined_a384_bucket.py`.

This note extracts the reusable theorem behind the `A=384` packet.  It does
not count the `F_17^32` roots, and it does not prove a threshold.  Its purpose
is to close the abstract deficiency-one chart reduction so future packets know
exactly which algebraic objects remain.

## Setup

Let `F` be a field, let `H` be the full root set of `X^n - 1` in `F`, and
assume `char(F)` does not divide `n`.  Fix an exact-agreement bucket with

```text
t = A-k,     j = n-A,     t = j.
```

The Hankel split has shape `t x (j+1) = j x (j+1)`.  Write it as an affine
pencil

```text
M(Z) = M0 + Z M1.
```

For `0 <= i <= j`, let `M_i(Z)` be the maximal minor obtained by deleting
column `i`, and set

```text
c_i(Z) = (-1)^i M_i(Z),
L_Z(X) = sum_i c_i(Z) X^i.
```

Assume the pencil is not identically rank deficient, equivalently some
`M_i(Z)` is not the zero polynomial.

## Theorem

Every finite slope lies in one of three charts.

1. **Rank drop.** All `c_i(z)` vanish.  Then `rank M(z) < t`.  The slope lies
   in the common zero locus of the maximal minors, hence in the zero set of
   any chosen nonzero `M_i`.  Since each matrix entry is affine in `Z`, every
   `M_i` has degree at most `t`.

2. **Full-rank low-degree chart.** The matrix has rank `t` and `c_j(z)=0`.
   The kernel is one-dimensional and generated by the Cramer vector
   `c(z)`.  Therefore every kernel locator has degree `< j`; this chart
   contributes no new exact-`A` degree-`j` locator.  If the lower-degree
   locator splits over `H`, it belongs to a higher-agreement bucket.

3. **Full-rank top chart.** The matrix has rank `t` and `c_j(z) != 0`.
   Then the kernel is spanned by the degree-`j` locator `L_z`.  Since
   `X^n - 1` is squarefree with root set `H`, this locator is valid exactly
   when

   ```text
   L_z(X) | X^n - 1.
   ```

   On the top chart this is equivalent to vanishing of the pseudo-remainder
   coefficients in the division of `X^n - 1` by `L_Z(X)`.  The pseudo-division
   has

   ```text
   delta = n-j+1
   ```

   steps, so each pseudo-remainder coefficient has `Z`-degree at most

   ```text
   delta * t = (n-j+1)t.
   ```

For a generic rank-drop point with `rank M(z)=t-1`, the local kernel has
projective dimension one.  After choosing a nonzero top pivot in the kernel,
valid degree-`j` locators are cut out by the pseudo-remainder equations of a
one-parameter monic locator pencil, each of degree at most `n-j+1` in the
kernel parameter.  Higher kernel dimension and identically valid pencils are
honest residual branches.

## Specializations

For the toy acid row `F_97`, `n=16`, `k=8`, `A=12`:

```text
t = j = 4,
delta = 13,
top coefficient degree bound = 52,
resultant degree bound = 64.
```

The verifier checks the theorem against exhaustive direct enumeration of all
`binom(16,4)=1820` degree-four subgroup locators.

For the row `RS[F_17^32,H,256]`, `n=512`, `k=256`, `A=384`:

```text
t = j = 128,
delta = 385,
top coefficient degree bound = 49280,
rough resultant degree bound = 65536,
rank-drop/low-degree minor degree bound = 128,
generic rank-drop local degree bound = 385.
```

Thus the remaining full-row task is sharply defined: compute or label the
top-chart pseudo-remainder root table and the rank-drop kernel-pivot branches.
The low-degree chart is already deduped into higher agreement.

## Planted Top-Chart Overlap Pruning

The planted `F_17^32` top-chart packet uses a support `T subset H` of size
`j=128` and a perturbation direction

```text
v_m = m + 2.
```

Write

```text
S_m = sum_{x in T} x^m,
u_m = S_m - z0 v_m.
```

Let `R subset H` have size `j`, and let `L_R(X)=prod_{r in R}(X-r)`.
If `z=z0`, the recurrence equations force `R=T`: the residual is

```text
sum_{x in T} x^m L_R(x),     0 <= m < j,
```

and the Vandermonde matrix on `T` is invertible.

If `z != z0`, put `lambda=z-z0`.  The residual equations are

```text
sum_{x in T \\ R} x^m L_R(x)
  + lambda * ( L_R(1)(m+2) + L_R'(1) ) = 0,     0 <= m < j.
```

If `|T \\ R| <= j-2`, these equations are a linear dependence among at most
`j` columns of a confluent Vandermonde matrix: the exponentials `x^m` for
`x in T \\ R`, together with the possible columns `1^m` and `m 1^m`.  The
roots are distinct and `X^n-1` is squarefree, so this confluent Vandermonde
minor is nonsingular.  Hence all coefficients vanish.  This is impossible:
if `L_R(1) != 0`, the coefficient of `m 1^m` is nonzero; if `L_R(1)=0`, then
`1 in R` and squarefreeness gives `L_R'(1) != 0`.

Therefore every non-planted valid top-chart locator must satisfy

```text
|T \\ R| >= j-1,
equivalently |T cap R| <= 1.
```

For the real `F_17^32` packet this removes every locator sharing at least two
roots with the planted support.  The remaining top-chart residual is the
near-disjoint case `|T cap R| in {0,1}`.

The verifier checks the analogous `F_97/mu_16` model by brute force over all
`binom(16,4)=1820` degree-four subgroup locators and all 97 slopes.  In that
toy model the only valid pair is the planted slope and planted support.

## Support-Only Residual Reduction

The same equations also remove slope multiplicity from the residual.  For a
fixed candidate root set `R`, define

```text
A_m(R) = sum_{x in T\\R} x^m L_R(x),
B_m(R) = L_R(1)(m+2) + L_R'(1).
```

Then `R` is valid at slope `z=z0+lambda` exactly when

```text
A_m(R) + lambda B_m(R) = 0,     0 <= m < j.
```

The row vector `B_m(R)` is never identically zero.  If it were zero for every
`m`, then `L_R(1)=0` and `L_R'(1)=0`; this would make `1` a double root of the
squarefree divisor `L_R | X^n-1`.

Therefore any valid support `R` determines at most one slope:

```text
lambda(R) = -A_m0(R)/B_m0(R)    for any m0 with B_m0(R) != 0.
```

The remaining top-chart condition is support-only:

```text
A_m(R) B_m0(R) - A_m0(R) B_m(R) = 0
for all m.
```

Together with the overlap pruning, the planted `F_17^32` top residual is:

```text
find R subset H, |R|=128, |R cap T| in {0,1},
such that the support-only consistency equations hold.
```

The verifier checks the `F_97/mu_16` analogue by comparing this support-only
test with a full brute-force search over all slopes and all subgroup locators.
They agree exactly; only the planted support at the planted slope survives.

## Overlap-One Exclusion

The support-only residual can be sharpened once we use that `1 in T`.
Suppose `z != z0` and `R` shares exactly one root with `T`.

The recurrence identity is an identity of linear functionals on polynomials
`Q` of degree `< j`:

```text
sum_{x in T\\R} L_R(x) Q(x)
  + lambda ( L_R(1) Q'(1) + (2L_R(1)+L_R'(1)) Q(1) ) = 0.
```

Use the Lagrange dual basis for evaluations on the distinct set `T`.

If the shared root is `y != 1`, then `1 notin R`, so `L_R(1) != 0`.  The
coefficient of the evaluation functional at `y` is

```text
lambda L_R(1) ell_y'(1),
```

where `ell_y` is the Lagrange basis polynomial for `T`.  Since `1 in T` and
`y != 1`, `ell_y'(1) != 0`; this is impossible.

If the shared root is `1`, then `L_R(1)=0` but `L_R'(1) != 0` because
`L_R` is squarefree.  The coefficient of evaluation at `1` is then
`lambda L_R'(1)`, again impossible.

Thus any non-planted valid top support must be fully disjoint from the planted
support:

```text
z != z0 and valid  ==>  R cap T = empty.
```

The verifier checks the `F_97/mu_16` analogue: all overlap-one supports are
rejected by the support-only equations, and the support-only equations still
match direct recurrence evaluation.
