import M31T16RaggedWitness.CountingRefutation
import M31T16RaggedWitness.RaggedWitness

/-!
Public entry point for the two independent support-level refutations of
universal depth-32 T16 alignment on the pinned Mersenne-31 quotient profile: the
explicit ragged collision witness (`RaggedWitness`) and the independent counting
refutation of the finite class census and integer gates (`CountingRefutation`).
-/
