# Statement-to-source correspondence

The package `experimental/lean/m31_flatness_conjecture_a1_s2/` is a
stdlib-only arithmetic and finite-enumeration shadow of the accompanying
threshold report.

| Lean declaration | Source object | Verification boundary |
|---|---|---|
| `integrated_floor_matches_cap` | The integrated whole-`T64` and mixed-`T16` neighbor packet | Recomputes the rooted packet and direct prefix predicate through the imported source definitions. |
| `mixed_remainder_geometry_exact` | Canonical complete-block removal from the eight mixed neighbors | Enumerates remainder lengths, available complete-block classes, and selector sizes. |
| `mixed_t64_t32_closure_exact` | Same-canonical-remainder closure at the saturated deficiency | Enumerates every complete-`T64` and complete-`T32` selector continuation and checks each candidate by direct locator multiplication; proves the distinct closure is exactly the integrated mixed list. |
| `reflected_e256_mixed_packet_exact` | The complementary selector choices in the same four remainder classes | Enumerates the reflected mixed packet, proves direct prefix agreement and disjointness from the whole-block family. |
| `reflected_e256_full_packet_exact` | Whole-block quadruple swaps plus the reflected mixed packet | Directly checks support validity, rooted deficiency, target equality, distinctness, and the combined census. |
| `selector_zero_and_deployed_e96_coexist` | Selector-spectrum zero prediction and deployed off-lattice witness | Reuses the two source theorems without identifying their different objects. |
| `selector_atlas_scope_arithmetic` | Complete-`T32` selector atlas | Projects the exact atlas summary and checks only the printed comparisons. |
| `typed_external_obstructions_coexist` | Comparison-domain and abstract-moment route cuts | Certifies the numerical coexistence model only; it is not an RS-realizability theorem. |
| `band_only_countermodel` | Band-versus-full-shell logical interface | Proves that a band cap alone does not imply any out-of-band cap. |
| `compiler_arithmetic_requires_out_of_band_input` | Coefficient-four arithmetic | Recomputes the covered-shell count, uncovered-shell count, closing total, reserve, and first failing uniform intercept. |

Every theorem is followed by `#print axioms`.  The five finite-enumeration
theorems use `native_decide`; the source aliases inherit the disclosed source
axioms; the remaining arithmetic theorems use ordinary proof terms, `decide`,
or `omega`.  There is no `sorry`, `admit`, custom axiom, or Mathlib import.
