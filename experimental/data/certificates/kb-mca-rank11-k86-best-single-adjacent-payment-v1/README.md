# Rank-eleven best-single adjacent payment at `K'=86`

This supplemental certificate extends the base-field-normalized
adjacent-support program from the closed rank-nine prefix `K'=10..85`
through `K'=86`.

The exact partition is one ordinary lane plus offsets `1..75`. A dedicated
checker accepts the two completed ordinary jobs retained from a broader
incomplete pilot. A paired raw scan proves that offsets `43..75` are entirely
safe and isolates exactly 415,413 unsafe units in offsets `1..42`. Paired
exhaustive carrier traversals then prove that every residual profile is paid
by its best individually valid single adjacent edge. The packet does not
compose overlapping edges.

```text
python3 experimental/scripts/verify_kb_mca_rank11_k86_best_single_adjacent_payment_v1.py
python3 experimental/scripts/verify_kb_mca_rank11_k86_best_single_adjacent_payment_v1.py --tamper-selftest
python3 experimental/scripts/verify_kb_mca_rank11_k86_best_single_adjacent_payment_v1_independent.py
```

Manifest SHA-256:
`d0869e5755252d08a59bfe763fd33c5032796ebc67ca410c7432d98d05762072`.
Primary and independent verifier SHA-256 values are
`a1078fb15349df3cf40000d90da98dfb5dcf8cf149b7f9a333b72121afd2b68a`
and
`fabb09251f7d0b453ed4f2110dee233cefe4294c9b1f473a44e5911f61ad1de3`.

The compact verifiers check source custody, the ordinary and offset coverage
identities, residual conservation, the exact premium ceiling, and the
component ledger. The manifest pins all paired finite waves and the
component-payment output; it does not embed the large raw captures.

This moves no deployed v4 atom, pays no remaining rank-eight or chronology
branch, and does not close error rank eleven, KoalaBear, or either prize
problem. The first open rank-nine row is `K'=87`; no adjacent extrapolation
is claimed.
