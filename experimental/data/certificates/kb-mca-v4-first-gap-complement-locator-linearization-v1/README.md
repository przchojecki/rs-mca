# KoalaBear first-gap complement-locator linearization

This packet verifies that the two first-gap source quotient tests are exactly
one two-dimensional complement-locator residue condition.

Replay:

```bash
python3 experimental/scripts/verify_kb_mca_v4_first_gap_complement_locator_linearization_v1.py --check
python3 experimental/scripts/verify_kb_mca_v4_first_gap_complement_locator_linearization_v1.py --tamper-selftest
```

The finite rows exhaust every candidate locator in their printed domains.
They are controls for the exact algebraic reduction, not evidence of a
deployed worst-case bound.

The packet makes no ledger movement. The growing-dimensional,
determinant-weighted residue-cylinder incidence theorem remains open.
