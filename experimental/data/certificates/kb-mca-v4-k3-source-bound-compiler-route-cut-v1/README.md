# KoalaBear v4 K3 source-bound compiler route cut v1

This packet is success condition **B**, not a K3 payment. Its compiler scope
is only the declared active residual slice `Z_BC^(m=2,r=4)`. It does not prove
a membership predicate for that slice or an exhaustive reduction from all
`Z_BC`. Within the declared slice it imports the proved raw
`433-1b -> O0a` exclusion, reconstructs all thirteen positive routes, and
records the first source-bound implication that cannot currently be made:

```text
declared-slice balanced-core bad slope
  -> total canonical selector into a supplied actual endpoint record /
     actual (4,4) component
  -> reconstruction and exact finite projection-fiber bound
  -> raw orientation workboard
  -> distinct affine slope under the same received line and owner
```

The selector/reconstruction/projection theorem is absent. A two-sided inverse
would be a sufficient stronger form, but is not required if a total selector
and exact projection-fiber bound are proved. The endpoint parameter line is
not the evaluation carrier. Therefore every requested K3 ledger integer is
left `null`; in particular, the joint `Q/BC/new` reserve is not silently
renamed `U_K3_allocation`.

The imported raw-zero routes have exactly zero surviving raw systems and a
`null` distinct-affine-slope payment. The source-cover figure `8` counts only
unresolved candidate row/passport combinations, not realized terminals or a
terminal-cell census. The `32099` partitions per supplied actual record are
bound directly to
`background/nodes/rate_half_kb_source_pencil_rank_transverse_compiler/statement.md`
at public-DAG commit `48a7de3c2d0d092b1899b1bb18d62bb4bf8861ce`
(content SHA-256
`0c1756ccf94e7b4503bf7b9aa5a39cebf16abaf1a200ec9496dd8bcf6b6f664f`).

The packet also freezes an exact independent obstruction to the proposed
`433-1b -> O0b` transport into the closed `O0a` route, scoped only to the
declared natural record/role/gauge/typed-incidence maps:

- on repeated BC, the guarded parallel-product ratio is `+1` for `O0b` and
  `-1` for `O0a`;
- on split BC, the full outside incidence signatures are non-isomorphic
  under all six outside-role permutations.

This does not rule out arbitrary orientation-changing algebraic maps. PR
#1155 is retained as a valid guard-transplant route cut and regression
dependency. It is not superseded or promoted to a slope payment.

The tangent manifest declares `U_paid=981104`, but that atom is not
transitively source-revalidated here: the tangent manifest/row expects
`experimental/grande_finale.tex` Git blob
`8a5d9791900ca9eed773feba146b92ad296704ce`, while PR #1152 has
`6b21d6ea937a8a9f85fc7ade6032d73efd4c7222`. Both normal and optimized
tangent-adapter verifier modes fail the `active_v4` source-hash gate.
The separately recorded `agents.md` steering drift is non-gating and is not
used as proof evidence.
Accordingly `981104` and the resulting reserve are manifest-declared exact
arithmetic, not banked by this packet.

Replay from the repository root:

```bash
python3 experimental/scripts/verify_kb_mca_v4_k3_source_bound_compiler_route_cut_v1.py
python3 -O experimental/scripts/verify_kb_mca_v4_k3_source_bound_compiler_route_cut_v1.py
/usr/local/bin/sage experimental/scripts/verify_kb_mca_v4_k3_source_bound_compiler_route_cut_v1.sage
~/math_code/.venv/bin/python experimental/scripts/verify_kb_mca_v4_k3_source_bound_compiler_route_cut_v1_flint.py
```

For full external source binding, also pass checkouts containing public-DAG
commit `48a7de3c2d0d...` and PR #1155 commit `1cf13bb4058d...`:

```bash
python3 experimental/scripts/verify_kb_mca_v4_k3_source_bound_compiler_route_cut_v1.py \
  --source-root /path/to/rs-mca-prize-dag \
  --pr1155-root /path/to/rs-mca-at-pr1155
```

Replay the imported raw certificate directly:

```bash
python3 experimental/data/certificates/kb-mca-v4-433-1b-raw-workboard-close-v1/verify.py \
  --source-root /path/to/rs-mca-prize-dag
```

Replay PR #1155 from a checkout at commit
`1cf13bb4058da19c5108bf79472394a598217bca`:

```bash
python3 experimental/scripts/verify_kb_mca_v4_433_1b_cell11_signed_pair_route_cut_v1.py
python3 -O experimental/scripts/verify_kb_mca_v4_433_1b_cell11_signed_pair_route_cut_v1.py
```

The tangent-adapter commands below are expected to fail at PR #1152 on the
stale `active_v4` source hash; they are source-gate diagnostics, not packet
passes:

```bash
python3 experimental/scripts/verify_kb_mca_v4_tangent_source_adapter_v1.py --check
python3 -O experimental/scripts/verify_kb_mca_v4_tangent_source_adapter_v1.py --check
```

The shipped JSON is canonical-payload hashed, source-bound, and checked by
a hostile mutation suite. The value `11304` is only a conditional candidate
abstract-label-orbit workload. The pinned `label_orbits.py` says it is not a
census or elimination, and it is not a representative count or payment.

The open-PR discussion in the companion note is a non-load-bearing temporal
snapshot. The certificate's durable overlap claim is only that no missing
bridge appears in its pinned dependency set. Likewise, leaving the dirty
cover/transport worktree untouched is an operational author audit, not a
proof claim or certificate input; those files are preserved and neither
refuted nor deleted.
