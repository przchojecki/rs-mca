# X1 / E5 multiscale resonance evidence

This directory contains the deterministic JSON artifact emitted by:

```bash
python3 experimental/scripts/verify_x1_e5_multiscale_resonance_evidence.py --emit
```

The packet is EXPERIMENTAL / AUDIT evidence for the Fable E5
`payment_completeness` red-team question.  It records no hidden rank beyond
deduped per-scale character-line payments in the pre-registered linear periodic
model.
