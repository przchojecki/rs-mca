# Complete literal source-line coverage of the saturated diagonal c2(1,1,2) branch

The JSON certificate in this directory summarizes 85 PROVED
nodes of the public campaign repository
https://github.com/AllenGrahamHart/rs-mca-prize-dag at commit 594aaa985568.
Each node carries its own machine verifier (verify.py, sha256-pinned
in the certificate); the replay command is recorded in the
certificate's provenance block. All verifiers were replayed by the
campaign coordinator at the pinned commit.

This packet moves no ledger value and does not close K3 or the
KoalaBear row. Fresh independent proof review remains required
before promotion to a GREEN banked result.
