# KoalaBear v4 common-core shortening staircase route cut certificate

This packet proves a same-record cancellation theorem for the common
intersection of one selected family of maximal agreement supports and freezes
the first exact KoalaBear payment walls.  It also proves why neither deleting
the common factor nor applying the published agreement-set shortening
transfer closes the active v4 S/A/E route.

The packet deliberately reports zero global-ledger movement.  Its strongest
output is the exact terminal partition for one fixed explanation family and
the missing line-level selector theorem needed to turn that local result into
a chronology-correct payment.

Run:

```sh
python3 -B experimental/scripts/verify_kb_mca_v4_common_core_shortening_staircase_route_cut_v1.py --check --tamper-selftest
python3 -O experimental/scripts/verify_kb_mca_v4_common_core_shortening_staircase_route_cut_v1.py --check --tamper-selftest
/usr/local/bin/sage experimental/scripts/verify_kb_mca_v4_common_core_shortening_staircase_route_cut_v1.sage
python3 -B experimental/scripts/verify_kb_mca_v4_common_core_shortening_staircase_route_cut_v1_flint.py
```

The Wolfram script is an independent exact-integer replay and may be run
through the project's Wolfram Cloud wrapper.

No K3 result, witness multiplicity, support count, or endpoint label is
treated as a distinct affine slope.  The packet does not claim S, A, E,
KoalaBear, or universal prize closure.
