# XR E3 Calculus

This directory contains the replayable certificate for
`experimental/notes/roadmaps/xr_e3_calculus.md`.

Replay:

```text
python3 experimental/scripts/verify_xr_e3_calculus.py
```

Regenerate:

```text
python3 experimental/scripts/verify_xr_e3_calculus.py --emit
```

The packet verifies the endpoint `C_2` consistency with averaged-XR and exact
killed `K_3` formulas for fixed-core and block-profile structured families.

