# KoalaBear v4 tangent-deep-source-rational-C5/base certificate

This certificate freezes the direct active-v4 successor that inserts the
joint pair-global projective-base C5/residual-base owner after tangent,
deep-MCA, and source-rational deletion.

```text
tangent charge               981104
deep-MCA charge              349526
source-rational cap         2078733
joint C5/base cap       2130706434
known total              2134115797
remaining budget 274980725977279290
```

Replay:

```sh
python3 experimental/scripts/verify_kb_mca_v4_tangent_deep_source_rational_c5_adapter_v1.py --check
python3 experimental/scripts/verify_kb_mca_v4_tangent_deep_source_rational_c5_adapter_v1.py --tamper-selftest
```

On the intrinsic-base stratum, C5 owns the entire incoming residual. On the
other stratum, the same joint cell owns every remaining base slope. The
uniform charge is `max(p+1,p)=p+1`. The packet does not classify local
base-defined pencils inside a pair whose intrinsic projective syndrome field
is larger than the base field.
