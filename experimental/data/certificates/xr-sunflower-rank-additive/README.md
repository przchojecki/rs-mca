# xr-sunflower-rank-additive certificate

This directory pins the toy linear-algebra recomputation for DAG node
`xr_sunflower_rank_additive` (2c-beta-1).

Run:

```bash
python3 experimental/scripts/verify_xr_sunflower_rank_additive.py
```

The verifier exhausts the packed sunflower triples recorded in
`toy_sunflower_rank.json`.
