# XR exchange-energy scaffold certificate

This packet supports the Fable E2 / QX.1 evidence item.  It verifies exact
Johnson-graph walk-energy identities for structured toy subsets and records the
quotient caveat that full-block strata have zero ordinary one-exchange energy.

Replay:

```bash
python3 experimental/scripts/verify_xr_exchange_energy_scaffold.py --emit
```

Artifact:

```text
xr_exchange_energy_scaffold.json
```
