# Row-sharp Q full-ladder dictionary certificate

This directory is generated by
`experimental/scripts/verify_rowsharp_q_full_ladder_dictionary.py`.

The certificate records a replay of the log-derivative dictionary:
vanishing first power sums through depth `t` are equivalent to vanishing
reversed-locator coefficients at exactly the `q`-free indices
`1 <= i <= min(t,b)`.

Status: `PROVED` dictionary lemma / `AUDIT` replay packet.  It does not
prove row-sharp Q, a max-fiber bound, image-scale MI/MA, or a finite
adjacent upper ledger.
