# M31 rank-seven proper-\(G\) zero-excess incidence route cut v1

This packet sharpens the unique \(Q=147\,595,\ k=4\,981\) residual left by
the source-basis overlap packet.  A class exceeding the closing cap must
have zero common-direction zeros, full normalized-label multiplicity,
actual affine rank six, and at least \(2\,157\,929\) zero-excess proper-\(G\)
members.  Seven affinely independent members from that last portion force

\[
\deg\gcd(G_i,G_j)\ge228\,652
\]

for some pair, together with the exact reduced determinant and common-\(H\)
incidence equations.

The packet also gives an exhaustive \(GF(31)\) source-compatible
counterfixture with \(60\,166\) proper members and largest fixed-\(G\)
slice \(5\,505\).  It disproves a one-slice aggregate bound and every
universal aggregate multiplier strictly below the exact ratio
\(60\,166/5\,505\) under the listed gates.  It does not rule out every
field-uniform aggregation theorem.  The deployed cap \(2\,157\,928\)
remains an explicit missing incidence theorem.

Replay:

```bash
python3 experimental/scripts/verify_m31_rank7_proper_g_zero_excess_incidence_route_cut_v1.py --check
python3 -O experimental/scripts/verify_m31_rank7_proper_g_zero_excess_incidence_route_cut_v1.py --check
python3 experimental/scripts/verify_m31_rank7_proper_g_zero_excess_incidence_route_cut_v1.py --tamper-selftest
python3 -O experimental/scripts/verify_m31_rank7_proper_g_zero_excess_incidence_route_cut_v1.py --tamper-selftest
python3 experimental/scripts/verify_m31_rank7_proper_g_zero_excess_incidence_route_cut_v1_independent.py
HOME=/tmp sage experimental/scripts/verify_m31_rank7_proper_g_zero_excess_incidence_route_cut_v1.sage
```

The packet does not pay \(Q=147\,595\), assign a v4 owner, move the active
ledger, extrapolate the toy fixture to M31, treat rank at least eight, or
close the M31 LIST row.

The JSON schema closes the top-level envelope only.  The primary verifier
closes every nested key and value against its trusted template, checks
candidate binding identities and paths before candidate-selected source
I/O, rejects symlinks and resolved paths outside the repository, hashes
only capped regular files through a streaming reader, and bounds JSON size
and nesting.
