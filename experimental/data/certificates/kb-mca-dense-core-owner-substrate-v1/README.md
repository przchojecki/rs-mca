# KoalaBear dense-core owner substrate v1

This certificate packages four proved inputs for the chronology-correct
owner route left open by PR #1168:

- exact repricing of the separate `2w` near-rational reserve;
- an explicit counterexample to unguarded `k -> k+1` badness transport;
- the repaired degree and same-support noncontainment guards;
- one typed deployed pole-line witness that passes those guards.

The primary verifier derives the endpoint tables, field and subgroup facts,
one-coefficient shift gap, toy exact-support census, and typed-witness root
margins. The independent verifier replays the load-bearing arithmetic and
algebra by a separate path. The mutation mode rejects changes to prices,
guards, source pins, witness ownership, and the #1168 bridge terminal.

No owner theorem, rank-eleven payment, active-v4 movement, or KoalaBear
closure is claimed.
