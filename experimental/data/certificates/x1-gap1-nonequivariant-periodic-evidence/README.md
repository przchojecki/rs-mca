# X1 GAP-1 non-equivariant periodic evidence

This directory contains the JSON artifact generated by:

```bash
python3 experimental/scripts/verify_x1_gap1_nonequivariant_periodic_evidence.py --emit
```

The packet is experimental evidence for Fable item E6.  It reports exact
linear-image ranks for non-equivariant `K_M`-periodic support models and checks
for amplification beyond the per-character product prediction.
