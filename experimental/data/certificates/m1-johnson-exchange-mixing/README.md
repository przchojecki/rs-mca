# M1 Johnson exchange-mixing certificate

This directory contains the deterministic JSON artifact emitted by:

```bash
python3 experimental/scripts/verify_m1_johnson_exchange_mixing.py --emit
```

The packet verifies the Johnson one-exchange and endpoint exchange-mixing
bounds, point-dictator equality, an exhaustive `J(6,2)` check, structured
examples, and seeded random families.
