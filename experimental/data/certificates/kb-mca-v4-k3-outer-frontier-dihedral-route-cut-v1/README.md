# KoalaBear K3 outer-frontier and dihedral route-cut certificate

This YELLOW packet performs three exact tasks:

1. reconstructs the committed per-record transverse frontier
   `26>22>18>12>8>3>2`;
2. proves that the live active-v4 four-cell first-match partition is already
   present, while recording the four additional semantic gates needed for a
   conditional any-69 cap; and
3. proves a fixed-point-free deployed-carrier dihedral counterexample to the
   inference that two carrier-preserving quadratic folds automatically give
   bounded-degree strict progress.

The final local types `(m,r,delta)=(2,4,2),(2,8,1)` remain open.  The packet
constructs no actual received-line record and moves no ledger value.  No live
source-to-slope theorem on this branch converts the conditional class cap into
a charge.

## Replay

```text
python3 experimental/scripts/verify_kb_mca_v4_k3_outer_frontier_dihedral_route_cut_v1.py --check --tamper-selftest
python3 experimental/scripts/verify_kb_mca_v4_tangent_source_adapter_v1.py --check
python3 -O experimental/scripts/verify_kb_mca_v4_tangent_source_adapter_v1.py --check
sage experimental/scripts/verify_kb_mca_v4_k3_outer_frontier_dihedral_route_cut_v1.sage
'/Applications/Wolfram Engine.app/Contents/Resources/Wolfram Player.app/Contents/MacOS/WolframKernel' -script experimental/scripts/verify_kb_mca_v4_k3_outer_frontier_dihedral_route_cut_v1.wl
```

The Python replay binds all ten live payloads and rejects mutations of the
base head, every payload pin, frontier, route rank, field prime, generator,
quadratic-fibre and common-zero-fibre facts, fixed-point-free and fixed-field
facts, active partition, semantic gates, historical authority, and ledger
status.  It directly gates the exact-base `grande_finale.tex` blob and runs
the repaired tangent verifier under both normal and optimized Python.  Sage
independently replays the finite-field, group, quotient-fibre, invariant,
zero-fibre, and fixed-field degree arithmetic.  Wolfram imports the source
certificates and derives the frontier and four-cell partition before
independently replaying the same arithmetic and the new JSON fields.

## Provenance seam

The predecessor tangent manifest payload
`536b08f23e552b2cb4fb226d74d42d54750118dae2efe619ff6e67d69d0370bf`
and row payload
`a05d8caf2b772c9c5cc7e7683631c9db7f5b3f2ba4cf8f436f6d3b16f4bdc189`
pinned the pre-`b13de8113` `grande_finale.tex` blob.  This packet repairs that
pin and binds the refreshed live payloads printed in the certificate.  The
off-branch abstract compiler at `ceb6611e7` is provenance only and is never
loaded as authority.
