# Rank-eleven adjacent-support carrier payment at `K'=83`

This supplemental certificate extends the base-field-normalized split-pencil
census from the closed rank-nine prefix `K'=10..82` through `K'=83`.

The analytic addition is an adjacent-flat circuit inequality for finite
matroids and its fixed-union specialization. A fixed `g`-dimensional
vanishing space couples every adjacent support pair `d/(d+1)` with
`d<=g-1`; bounds on support-disjoint pairs may be added. Together with the
single-carrier endpoint, this repairs the exact `K'=83` frontier without a
speculative triple-carrier premise.

```text
python3 experimental/scripts/verify_kb_mca_rank11_k83_adjacent_support_payment_v1.py
python3 experimental/scripts/verify_kb_mca_rank11_k83_adjacent_support_payment_v1.py --tamper-selftest
python3 experimental/scripts/verify_kb_mca_rank11_k83_adjacent_support_payment_v1_independent.py
```

Manifest SHA-256:
`99e01210543223ae45aa8e1f88a49c11951a0b37fd2f3496c1ce1e886d04a3fd`.
Primary and independent verifier SHA-256 values are
`628e94a8327964c7530e22608d7ea551c1d959beabafe7d829dc7071608ee81b`
and
`4e2d46a39ea5e711db59f29d65f137c55d266f56970dbf4d7a37669cc601d8e1`.

The previous packet's `K'=83` pairwise-atlas wall remains a valid method
limitation. This packet supersedes only its frontier conclusion: `K'=83` is
now closed and the first open rank-nine row is `K'=84`.

This moves no deployed v4 atom, pays no remaining rank-eight or chronology
branch, and does not close error rank eleven, KoalaBear, or either prize
problem.
