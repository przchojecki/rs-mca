# Rank-eleven raw-clipped adjacent payment at `K'=87`

This supplemental certificate extends the base-field-normalized
adjacent-support program from the closed rank-nine prefix `K'=10..86`
through `K'=87`.

The reusable structural step intersects independent selected-incidence caps
with the fixed-union adjacent-support census. Each incidence cap is divided
by its exact eleven-set extension factor and floored before entering the
circuit polytope. The resulting rational weighted optimum is evaluated by
dual fractional-knapsack orders. Only support-disjoint adjacent pairs may be
composed; no overlapping pair bounds are added.

The exact finite partition is one ordinary lane plus offsets `1..76`. Fresh
paired ordinary traversals cover the complete ordinary lane. Paired raw scans
prove that offsets `44..76` are entirely safe and isolate exactly 511,677
unsafe units in offsets `1..43`. Paired raw-clipped carrier traversals then
price all 77,179,660 residual profiles per implementation.

```text
python3 experimental/scripts/verify_kb_mca_rank11_k87_raw_clipped_adjacent_payment_v1.py
python3 experimental/scripts/verify_kb_mca_rank11_k87_raw_clipped_adjacent_payment_v1.py --tamper-selftest
python3 experimental/scripts/verify_kb_mca_rank11_k87_raw_clipped_adjacent_payment_v1_independent.py
```

Manifest SHA-256:
`ec81aa702181ba7a683e9f2a1afeb942a968c7d0232d6e3e7f3e44669d3f0f8a`.
Primary and independent verifier SHA-256 values are
`da8e97ab5a3b8778acd4415b598e5086f8bd5942c275ad4644c45843cfbc0b40`
and
`4f0c45d41bd6282fb167fe577e2428ecc9df11aab03ec81f37d1156bff9ad9be`.

The compact verifiers check source custody, the raw-clipped theorem in both
allocation orientations, ordinary and offset coverage identities, residual
conservation, the exact premium ceiling, and the component ledger. The
manifest pins the paired finite waves and component-payment output; it does
not embed the large raw captures.

This moves no deployed v4 atom, pays no remaining rank-eight or chronology
branch, and does not close aggregate error rank eleven, KoalaBear, LIST, MCA,
or either prize problem. The first open rank-nine row is `K'=88`; no adjacent
extrapolation is claimed.
