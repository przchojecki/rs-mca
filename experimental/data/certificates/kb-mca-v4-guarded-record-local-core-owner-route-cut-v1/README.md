# KoalaBear v4 guarded record-local core-owner route cut v1

This certificate proves an exact GF(11) actual-record collision and checks the
new degree/near-rational guard layer.  It refutes record-local core ownership
and checks the global-core-first compiler: a nonempty global core invokes one
#1163 fixed family, while an empty global core is retained as an explicit
residual.  It also checks the degree-31 coherent-forest fence, exact
31-overlap correction-ray transport, and the imported selector-free
all-LineRay budget wall at affine ranks three/four.  The packet does not pay
the staircase, local-empty-core, or high-rank ray residuals; all deployed
ledger movements remain zero.

The GF(11) collision and whole-line global-core router are replayed prior
public-DAG work.  The new theorem boundary is the degree-31/order-32
empty-core coherence fence, guarded correction-ray compiler, and low-rank
aggregate gate.  The later public-DAG nonempty-core composition and PR #1156
clone branch are explicitly out of scope.

Replay:

```bash
python3 experimental/scripts/verify_kb_mca_v4_guarded_record_local_core_owner_route_cut_v1.py --check
python3 -O experimental/scripts/verify_kb_mca_v4_guarded_record_local_core_owner_route_cut_v1.py --check
python3 experimental/scripts/verify_kb_mca_v4_guarded_record_local_core_owner_route_cut_v1.py --tamper-selftest
HOME=/tmp TMPDIR=/tmp /usr/local/bin/sage experimental/scripts/verify_kb_mca_v4_guarded_record_local_core_owner_route_cut_v1.sage
/Users/scott/math_code/.venv/bin/python experimental/scripts/verify_kb_mca_v4_guarded_record_local_core_owner_route_cut_v1_flint.py
```

Regenerate the canonical manifest after intentional packet changes:

```bash
python3 experimental/scripts/verify_kb_mca_v4_guarded_record_local_core_owner_route_cut_v1.py --write
```
