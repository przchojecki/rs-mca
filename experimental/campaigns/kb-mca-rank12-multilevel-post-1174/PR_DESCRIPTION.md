## Summary

- prove a sharp all-level scalar-resource / selected-support first-moment
  barrier at the first open KoalaBear affine-error-rank-twelve row;
- identify the exact actual-line local-polynomial gluing quotient associated
  to a family of minimizing pair cores;
- pay the complete quotient-rank-zero branch by
  `49,106,899,082,787,469`, with
  `199,599,500,258,500,901` slack to the rank-ten child target;
- isolate quotient ranks one and two as the exact remaining
  source-realizable realization terminals.

## Exact barrier

The complete truncated-margin resource permits a balanced abstract
histogram whose selected-support core incidence forces only
`120,205,662,451,376,300` records through one coordinate.  This misses the
rank-ten target by `128,500,736,889,912,070`.  Even granting every record
all `1,116,048` selected-support core incidences averages only
`146,337,362,121,160,346`.

The fixed-pair singleton cap and current interleaved pair-type cap do not
repair this relaxation: the margin-one part needs only `230,227,321,946`
types versus the allowed `12,761,830,235,484`.

This is a method barrier, not an RS counterexample.

## Actual-line compiler

For complete minimizing pair cores `H_gamma` with union `U`, the packet
proves

```text
(sum A_H)^perp = locally polynomial sections on all H,
A_U^perp       = global degree-<K sections on U.
```

The received pair therefore has quotient image rank zero, one, or two.
Rank zero is paid by splitting raw-low and raw-high records: low records
inject through distinct exception coordinates outside a common complete
pair core, while high records consume `d+1` units of the nonuniform margin
resource.  Incremental `K`-overlap forces generation and routes to this paid
rank-zero terminal.

The exact residual is quotient rank one (one projective scalar combination
global on `U`) or quotient rank two (none global on `U`).

## Controls and scope

- a cyclic construction balances the scalar incidence extremizer;
- a `GF(11)` post-near quotient-rank-zero record prevents the false
  substitution `n-m=w`;
- the public SYZ25 three-core example prevents promotion of nominal excess
  to supported-dual generation;
- Python normal/optimized replay and eight hostile mutations pass;
- Sage positive and negative gluing controls pass;
- the full `grande_finale.tex` build passes.

This packet moves no active-v4 ledger atom, does not pay quotient ranks one
or two, does not pay affine error rank twelve, and does not close KoalaBear.
It is stacked on exact #1174 head `1b613fc669158a690a52b64f0eeb440f10672f1e`
and remains conditionally dependent on divergent #1160 commit
`c5f4ea7a0c78828c901ae5f3428894a8b2e2806b`.  It must be replayed on the
integration tree containing both dependencies before integration.

The old locator/four-block numerical chain is not imported: its required
rank-two load came from a withdrawn descent and is not supplied by #1174.
