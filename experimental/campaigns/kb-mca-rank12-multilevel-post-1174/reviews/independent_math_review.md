# Independent mathematics and certificate review

Date: 2026-08-20

Reviewer: isolated Codex mathematics-audit subagent

## Statement audited

The initial-row KoalaBear rank-twelve packet proves two scoped results.

1. The complete scalar truncated-margin resource, even optimized across all
   margin levels, cannot force the existing rank-ten child by one-coordinate
   averaging.  The sharp relaxed forced load is
   `120205662451376300`, short by `128500736889912070`.
2. For an actual selected core family, the supported-dual span defect is the
   local-polynomial gluing quotient.  Its received-pair image has rank
   `c in {0,1,2}`.  Under the printed initial-row, exact-support,
   `dim(C')=11`, and `|U|>=m` guards, the complete `c=0` branch has at most
   `49106899082787469` slopes.  Only `c=1,2` remain.

## Files and source sections read

- `experimental/grande_finale.tex`, the new all-level scalar barrier,
  pair-count remark, actual-line gluing-defect theorem, proof, and scope
  remark.
- `proofs/scalar_resource_first_moment_barrier.md`.
- `proofs/actual_line_core_generation_route.md`.
- `experimental/scripts/verify_kb_mca_rank12_scalar_resource_barrier_v1.py`.
- Both Sage controls in this campaign.
- The exact #1174 raw-margin resource and shortening statements.
- The separately pinned #1160 same-support near-rational repair.
- The public SYZ21--SYZ25 gluing criterion summarized at pinned commit
  `acfa4f072d0c7fe8e706c80d8e21cb5b083f73a6`.
- Live PR #1170's declared scope for overlap comparison.

## Dependency classification

- **PROVEN:** the #1174 nonuniform support-margin resource and rank-ten child
  target.
- **IMPORTED:** #1160's disjoint `2d` near-rational deletion.  It remains a
  divergent exact dependency of the post-near unsafe load.
- **PROVEN / imported geometry:** the SYZ local-to-global gluing criterion and
  its coplanar hostile control.
- **PROVEN in this packet:** the scalar linear-program extremizer, cyclic
  balanced-incidence realization, actual received-pair quotient trichotomy,
  exact rank-zero payment, and incremental-`K`-overlap route.
- **OPEN:** payments for the `c=1` and `c=2` actual-line terminals.

## Independent proof audit

The orthogonal-complement identities are correct.  A zero-extended
annihilator supported on `H` has perpendicular equal to the functions whose
restriction to `H` belongs to the restricted RS code.  Intersecting over
the cores gives `A^perp=L`; taking `H=U` gives `W^perp=C_K(U)`, and the
dimension identity follows because `A` is contained in `W`.

The source now binds every pair core to the actual selected record: an exact
support `S_gamma`, its explanation `h_gamma` in one affine translate
`h_*+C'`, a raw-margin minimizing `b_gamma in C'`, and
`a_gamma=h_gamma-gamma*b_gamma`.  Therefore

```text
|S_gamma intersect H_gamma| = m - raw_margin_gamma.
```

In quotient rank zero, global degree-less-than-`K` polynomials `a,b` match
the received pair on `U`.  A raw-low record has at least `m-d=K` points in
its selected pair core, so RS uniqueness identifies its selected pair with
`(a,b)`.  Its explanation is therefore `a+gamma*b`.  Same-support pair
noncontainment forces a nonempty exception outside the complete common core,
and one outside coordinate supports at most one finite slope.  Hence the
raw-low count is at most `n-m=981104`.

If that count is `L0`, every other record costs `d+1=67473` resource units,
while every raw-low record costs at least one.  Thus

```text
L <= L0 + floor((C_11-L0)/(d+1)).
```

The integer expression is nondecreasing in `L0`; its endpoint at `981104`
is exactly `49106899082787469`, with child-target slack
`199599500258500901`.  No near-rational inference is used in this payment.
The GF(11) control correctly demonstrates why the discarded inference
`c=0 => near` was false: `n-m` and `m-K` are different quantities.

For incremental overlap, each new local polynomial agrees with the already
glued representative on at least `K` distinct evaluation points.  RS
uniqueness identifies them, so `L=C_K(U)`, equivalently `A=W` and `c=0`.
Under the preceding initial-row guards this reaches the paid terminal.

The scalar optimizer is also correct.  Starting each record at truncated
margin one, promoting a record to the capped class costs `d` additional
units while it can erase `m-1` incidences; an ordinary extra unit erases only
one.  Since `(m-1)/d>1`, capped promotions come first.  The resulting exact
histogram and cyclic residue assignment prove sharpness inside the declared
relaxation.  The stronger fantasy calculation is correctly scoped to
selected-support incidences, not complete pair cores.

## Independent replay

The following checks passed on the reviewed bytes:

```text
Python normal:                         PASS, 8/8 mutations
Python optimized (-O):                 PASS, 8/8 mutations
Canonical JSON payload hash:           PASS
Independent big-integer rederivation:  PASS
SYZ25 coplanar hostile control:         PASS
Incremental-overlap positive control:  PASS
GF(11) post-near c=0 regression:       PASS
git diff --check:                       PASS
pdflatex syntax pass to /tmp:           PASS, 126 pages
```

The independent exact values were

```text
C_11                              3313389801746721900417
capped records                    49103551414195675
residual resource                 56673
first-moment forced coordinate    120205662451376300
c=0 cap                           49106899082787469
c=0 child-target slack            199599500258500901
```

## Novelty and scope

The abstract dual/gluing equality is imported from the public SYZ chain.
The new content is the actual-record adapter, the exact rank-zero payment,
the routing of incremental overlap into that payment, and the all-level
scalar no-go.  PR #1170 treats conditional rank-eleven component geometry
and does not duplicate this initial rank-twelve route cut.

This packet moves no active-v4 atom, does not pay quotient ranks one or two,
does not pay affine error rank twelve globally, and does not close
KoalaBear.  The scalar extremizer is not claimed Reed--Solomon realizable.

## Parameter, layer-cake, and moment audit

All numerical constants are exact functions of the printed initial-row
parameters.  The field-order guard appears only in the pair-type comparison.
There is no layer-cake, dyadic summation, Markov, Chebyshev, or moment step.
The rank-zero numerical payment must not be transported to shortened rows
without recomputing the resource and union-size guards.

## Remaining publication risks

- #1160 and #1174 must coexist in the eventual integration tree before the
  whole post-near rank-twelve route can be advertised without an external
  dependency pin.
- The Markdown proof note contains several cosmetic parenthesized formulas
  where inline TeX delimiters would render better; this does not affect the
  source theorem.
- The next mathematical problem is the actual-record `c=1/c=2` population
  theorem, not another scalar threshold computation.

## Verdict

**GREEN — the local theorem, exact rank-zero payment, scalar barrier, and
stated route cut are mathematically sound under their printed guards.**

The source integration is PR-worthy as a conditional rank-twelve partial
payment/route-cut packet after dependency custody and a separate certificate
review.  It is not a rank-twelve or row closure.
