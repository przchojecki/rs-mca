# Frontier Map

## Current record

PR #1174 commit `1b613fc669158a690a52b64f0eeb440f10672f1e`
pays affine error rank eleven after the pinned #1160 near deletion.  Its
single-cutoff theorem proves that rank twelve would require at least
`546519697764383119` slopes, exceeding the available `274980728111260144`
by `271538969653122975`.

## Dependency spine

1. #1168 supplies canonical selected records and the nonuniform truncated
   support-margin resource.
2. #1160 supplies the external disjoint near-rational charge `2d=134944`.
3. #1174 supplies the raw-low heavy-core shortening theorem, the uniform
   rank-one cap, the exact rank-eleven child target, and the rank-twelve
   single-cutoff wall.
4. Old locator/four-block packets and public-DAG circuit/Segre routers are
   candidate mechanisms only until an actual-record adapter is proved.

## Named open joint

Exploit several raw-margin levels simultaneously.  The desired inequality
must couple the nested high-margin tails with intersections/collisions among
their minimizing pair cores strongly enough to force one rank-ten child of
size at least `248706399341288370`.  A separate acceptable joint is a
chronology-correct owner for the surviving dense cross-pair component.

The exact algebraic object is now frozen.  For an actual minimizing pair-core
family \(\mathcal H\), its supported-dual span defect equals the dimension of
the local-polynomial gluing quotient.  The actual received pair has quotient
image rank zero, one, or two.  On the guarded initial rank-twelve row, rank
zero is paid exactly by \(49106899082787469\), leaving slack
\(199599500258500901\) to the child target.  Incremental \(K\)-overlap forces
generation and therefore routes to that rank-zero payment.  The remaining
work is a quantitative first-match count of the rank-one and rank-two defect
classes.

## Nearby stronger and weaker statements

| statement | status | relation |
|---|---|---|
| rank eleven post-near payment | proved conditionally in #1174 | child target |
| rank twelve single-cutoff payment | refuted as a method | shortfall `271538969653122975` |
| multi-threshold cumulative payment | open | primary target |
| old common-locator/four-block payment | unaudited on #1174 | possible mechanism |
| active-v4 rank-twelve owner | open | stronger chronology target |

## Known barriers

- Truncated margin cannot be substituted for raw mismatch; the GF(11)
  regression has raw margin four and truncated margin three.
- Every single raw-low cutoff fails already at the initial rank-twelve row.
- The old three-deficiency `279911` figure was not a global capacity bound.
- Per-pair exception multiplicity can be sharp in constant-code star models.
- Arithmetic savings from ArkLib or a four-block extremal profile are not a
  theorem over the full admissible incidence polytope.
- The full scalar margin resource plus first-moment core averaging forces at
  most `120205662451376300` slopes through one coordinate, short of the
  rank-ten child target by `128500736889912070`.
- Adding fixed-pair singleton capacity and the interleaved pair-type cap does
  not repair the gap: the abstract extremizer needs `230227321946` pair
  types, versus the allowed `12761830235484`.
- Even the fantasy bound in which every selected record contributes all
  `1116048` selected-support core incidences averages only
  `146337362121160346` incidences per
  coordinate, still `102369037220128024` below the child target.  One-heavy-
  selected-support coordinate averaging is therefore the wrong object at
  rank twelve.
- The old locator chain assumes a rank-two family of size `5170912`.  That
  family came from the withdrawn rank-twelve descent and is not supplied by
  #1174, so the locator and four-block packets remain conditional modules.
