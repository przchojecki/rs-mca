# Frontier Map

## Current record

PR #1167 exact head `491ccdf53d54846f5a013b808960645275c64ed3`
proves the rank-ten margin/interleaving payment.  For explanation dimension
ten (error rank eleven), its exact one-threshold formula has minimum
`1040506078215897711` at `T=876`, above `B_*`.  This is a method wall, not an
unsafe instance.

## Dependency spine

1. #1166 head `b67078c7c0254ce9e54e5748634de5133fae98ef`:
   actual-record selection, intrinsic near deletion, support-local margin, and
   reversible error/explanation rank gauge.
2. #1167 head `491ccdf53d54846f5a013b808960645275c64ed3`:
   sub-square common-support interleaving collapse, high/low split, and the
   sharp fixed-pair ratio-fiber charge.
3. Active `experimental/grande_finale.tex`: ordinary affine-span list cap in
   the actual sextic line field.
4. GF(11) parallel-star certificate: the fixed-pair factor `n-A` is sharp
   under the local hypotheses.

## Named open joint

Control the aggregate load of *different* minimizing pairs.  Local margin,
one-pair ratio fibers, and cumulative ordinary/interleaved list sizes ignore
collisions among pair cores.  The missing input must couple distinct typed
pairs or prove that a high-load parallel bundle enters a named owner.

## Nearby stronger and weaker statements

| statement | exact status |
|---|---|
| post-near error rank at most 9 | paid by #1166 |
| post-near error rank 10 | paid by #1167 |
| error rank 11 by #1167 one-threshold split | fails; minimum `1040506078215897711` |
| weighted pair graph with parallel edges | candidate route cut, not payment |
| active-v4 S/A/E chronology | open and outside this local campaign |

## Known barriers

- A fixed pair can own exactly `n-A` post-near slopes; per-pair improvement is
  impossible without new global hypotheses.
- Weighted multigraph degree does not imply distinct neighbors because
  parallel slope records are legal.
- The sub-square collapse requires the sextic field; it fails over the base
  prime at the relevant list sizes.
- The complete core-deficiency layer cake was previously priced above budget;
  it must be independently replayed before being declared a barrier.
