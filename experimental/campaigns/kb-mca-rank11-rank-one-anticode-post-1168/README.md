# KoalaBear rank-eleven rank-one pair-anticode campaign

This campaign is stacked on PR #1168 at exact parent `6a5dcdae1591fc7f044eda6a942bfe178521a48c`.

It proves and certifies a cross-pair router for actual minimizing-pair
families whose `2 x s` coefficient matrices, `s<=10`, have pairwise rank
distance at most one. The two possible geometries are:

- fixed right factor: a common-core-aware affine correction ray, bounded by
  `8,147,918` distinct selected slopes;
- fixed left factor: an affine correction space of dimension at most ten,
  bounded by `1,031` slopes in the proper branch, with a precise
  positive-dimensional affine-linear terminal otherwise.

Pairwise complete-core intersection `K-1` forces the rank-one hypothesis.

Canonical payload SHA-256:
`7190997f4e6e464ce41aa3c8389968985686604ad81e9b272ca7c07130d323a5`.

This is a proved structural route cut with zero active-v4 ledger movement.
It does not pay all error-rank-eleven families and does not close KoalaBear.
