# Frontier Map

## Current record

`przchojecki/rs-mca` PR #1163 at exact head
`e26c15b2d2c2f98ae12dda17b97c40981f76e1ff` proves the reversible
same-record common-core shortening adapter and the exact KoalaBear staircase
walls.  It moves no global ledger value.

## Dependency spine

1. Active v4 exact-agreement reduction supplies actual support-wise bad
   records on one received line.
2. PR #1160 pays the full near-rational stratum by `2w`, with the identical
   witness support retained.
3. The guarded lattice adapter requires `s_k<=omega`; a `K=k+1` envelope alone
   is insufficient.
4. PR #1163 cancels one fixed local core and pays only a single fixed family in
   the declared staircase ranges.
5. The prior public-DAG global-core-first router `be4efd23a` applies #1163
   once when the selected supports have nonempty total intersection; the
   later composition `fc74e16c` sharpens that separate residual.
6. A coherent degree-31 source forest whose every order-32 subfamily has a
   core must itself have a global core.  Hence the remaining empty-core
   branch is exactly a local empty order-32 core or source noncoherence.
7. Across one 31-overlap, source noncoherence is exactly one correction-ray
   direction `P`; one ray is paid, but an aggregate direction theorem is open.
8. The integrated selector-free all-LineRay theorem pays the complete forest
   when the affine dimension of all actual error pairs is at most three.  The
   residual therefore has affine dimension at least four.

## Named open joint

On the remaining affine-rank-at-least-four forest, prove that all first
31-overlap passport changes share one paid correction-ray owner, or give an
exact budget-fitting bound on realized directions.  The coherent branch
already has one global core and invokes #1163 once.  A core, source
interpolant, or ray direction chosen independently for each local record is
not enough.

## Nearby stronger and weaker statements

| statement | status | consequence |
|---|---|---|
| one fixed-core cancellation | proved in #1163 | local reversible adapter |
| one fixed-family staircase bound | proved in #1163 | local payment only |
| local core is a slope invariant | refuted in public DAG `83eefd94f` | naive owner retired |
| line-global global-core priority | known/replayed from `be4efd23a` | invokes #1163 once when the total intersection is nonempty |
| coherent degree-31 forest with all local cores | proved conditionally | forces one global core |
| source coherence across 31-overlaps | open | exact remaining forest bridge |
| one 31-overlap passport change | proved conditionally | one genuine correction ray |
| aggregate correction-ray directions | open | required for noncoherent forest payment |
| all-pair error affine rank at most 3 | candidate imported payment | exact cap fits `B_*` |
| all-pair error affine rank at least 4 | open | current noncoherent residual |
| aggregate priority/forest bound | open | required for ledger movement |
| same-owner S/A/E maximum theorem | open | may bypass the forest |

## Known barriers

- Fixed-core payments cannot be summed over core choices.
- Jo shortening first fails catastrophically at `c=4131` (3765-bit
  multiplier).
- The exact fixed-family affine-span wall is `s=13/14`.
- Public-DAG commit `83eefd94f` gives an actual GF(11) record-local owner
  collision; its complete critical atlas and guarded status must be audited.
- General sunflower/spread decompositions have losses exponential in support
  size and do not automatically fit the deployed finite budget.
