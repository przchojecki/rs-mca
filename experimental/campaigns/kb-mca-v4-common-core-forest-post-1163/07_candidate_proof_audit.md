Statement audited:

The complete GF(11) actual-record atlas refutes record-local common-core
ownership after the actual-degree, same-support, global-affine, and
near-rational guards; it does not refute a line-global selector or assert
membership in the complete active-v4 first-match residual.  Conditional on
an already-declared first-match slope set and one canonical complete actual
record per slope, the global-core-first compiler partitions the whole line
without summing record-local core choices: a nonempty global core invokes
the #1163 adapter once, while an empty global core is emitted as one explicit
residual.  A second exact lemma proves that a common degree-31 source
interpolant plus nonempty intersection on every order-32 subfamily forces a
nonempty global core.  A third exact lemma identifies every source change
across a 31-overlap with one genuine complete correction ray.  The integrated
all-LineRay set-pair theorem then pays the whole selected forest when its
actual error affine dimension is at most three.

Files/sections read:

- `experimental/notes/thresholds/kb_mca_v4_guarded_record_local_core_owner_route_cut_v1.md`
- `experimental/scripts/verify_kb_mca_v4_guarded_record_local_core_owner_route_cut_v1.py`
- `experimental/grande_finale.tex`, active S/A/E and common-support sections
- PR #1160 and #1163 notes and certificates
- public-DAG commits `3f626c84d`, `d797d8ffd`, and `83eefd94f` as
  provenance/context only; no public-DAG object is imported by the manifest

Dependencies:

- PROVEN/REPLAYED: all seven displayed explanations are unique degree-`<5`
  explanations with exact seven-point maximal supports.
- PROVEN/REPLAYED: every displayed slope is same-support MCA-bad.
- PROVEN/REPLAYED: all shifted-lattice minima are `d1=3=w+1` and the actual
  locator pairs satisfy `s_k<=omega`.
- PROVEN/REPLAYED: the seven critical records have core histogram
  `{8,10}:1`, `{10}:5`, `{5,10}:1`; every slope has multiple local cores.
- PROVEN/REPLAYED: the full fixture has global core `{10}` and is not one
  affine explanation line.
- PROVEN: for a fixed first-match set and complete-record selection, the
  global support intersection is unique; if it is nonempty, the #1163
  fixed-core adapter is applied once to the entire family, and the `s<=2`,
  direction-separated `3<=s<=13`, unresolved `3<=s<=13`, and `s>=14`
  terminals are disjoint and exhaustive.
- PROVEN: a minimal empty-core cover has at least 33 members if every
  order-32 subfamily has a core; its private point produces 32 distinct roots
  of a nonzero coordinate error polynomial of degree at most 31, a
  contradiction.
- PROVEN: two coefficientwise degree-31 interpolants agreeing on 31 distinct
  slopes differ by `P(X)` times the 31-root overlap locator; at the new slope
  the locator is nonzero, so the identical selected record lies on the
  resulting correction ray.
- PROVEN/REPLAYED: the printed `1963173` correction-ray bound is used only at
  the primitive/no-common endpoint.  After canceling a locally automatic
  common subset by the direct coefficientwise/exchange extension of #1163,
  the exact multipart shortened-row formula has maximum
  `342921713716` at `q=3`, strictly below `B_*`; `q<=2` is fixed-family paid.
- IMPORTED/REPLAYED: the selector-free all-LineRay set-pair theorem counts
  every retained `(slope,explanation)` pair; in this packet there is exactly
  one complete pair per slope, and the rank-three cap is
  `157397034144292985 < B_*`, while rank four fails.
- IMPORTED/REPLAYED: #1160 is rejected by the necessary BC degree guard.
- UNPROVEN: survival of the toy fixture through every earlier active-v4 owner.
- UNPROVEN: payment of the #1163 staircase residuals.
- UNPROVEN: a common degree-31 source interpolant across the complete
  31-overlap forest.  The active source defines it only for one order-32 core.
- UNPROVEN: routing/payment of a local empty order-32 core or a noncoherent
  source forest on the deployed row.
- UNPROVEN: an aggregate first-match bound on the number or ownership of
  distinct correction-ray directions on the surviving all-pair affine-rank-
  at-least-four branch.  Per-edge ray bounds may not be summed.

Parameter dependence:

The counterexample is finite and scoped to `(p,n,k,m)=(11,10,5,7)`.  The
deployed arithmetic `2w=134944`, exception reserve `31`, and remaining
`274980728111260112` is exact calibration only.  No implication from the toy
row to deployed KoalaBear is asserted beyond falsifying the general
record-local ownership rule.

Layer-cake / dyadic summability:

Not applicable.

Moment / Markov / Chebyshev:

Not applicable.

Edge cases / notation:

- `k` is the actual code dimension; no silent `K=k+1` convention is used.
- Counts are distinct affine slopes, never records, supports, or core labels.
- The `2w` near-rational payment and 31-slope exception reserve are disjoint.
- A lexicographic complete-record selector is total.  The global-core-first
  compiler removes double charging for a nonempty global core, but it does
  not prove a payment for every resulting staircase terminal.
- The automatic ray core may be strictly smaller than the intersection of
  selected maximal supports.  The ray proof uses its explicit common-subset
  cancellation lemma and does not invoke #1163's exact-core statement.
- The route-cut terminal must not be labelled `UNPAID_PRIMITIVE` without
  checking every earlier owner.
- The all-LineRay handwritten theorem is not confused with its Lean module,
  which is explicitly an unproved statement target.

Numerical evidence:

The 2,000-system empty-global-core search and one-/two-swap neighborhood are
bounded negative evidence only.  They are not used in the theorem.
The four-support `GF(5)` degree-three example checks sharpness of the
degree/root boundary; the general result is the direct minimal-cover proof,
not extrapolation from that control.

Verdict:

YELLOW - OPEN GAP.  A fresh isolated mathematics review is GREEN on the
scoped finite route cut and Theorems 4.1--4.4 after the witness-interface,
common-subset cancellation, exact-maximum, provenance, and add-back repairs.
The packet remains YELLOW overall because those theorems have not been
inserted into the active S/A/E first-match chronology; ledger movement is
still zero.

Remaining risks:

The active first-match strips may own the toy fixture for a reason not tested
here.  The global-core-first theorem takes the chronology-correct residual
set as an input rather than deriving it from source.  Its nonempty-core
branch still inherits the unpaid #1163 staircase terminals, and its
empty-core terminal is reduced to `LOCAL_EMPTY_ORDER_32_CORE` or a sequence
of exact correction-ray transitions.  The selector-free rank-at-most-three
part is paid; the rank-at-least-four part has no aggregate bound.

Minimal next action:

Freeze the exact active-v4 first-match residual set to which Theorem 4.4
applies, insert its `2w+31` add-back at the correct chronology position, and
obtain an independent source/chronology review.  Then work only on the
all-pair affine-rank-at-least-four branch: prove one correction-ray owner or
an exact budget-fitting direction multiplicity.  Do not sum fixed-core or
per-edge ray payments.
