# Frontier map

## Exact parent

PR #1173 head 2788d5ec3fb4b1d6f9c43a58a86ec2381e5f6804.

## Repaired joint

The prior recurrence incorrectly treated the truncated support margin as a
raw mismatch count.  The corrected compiler fixes a raw cutoff \(T\le d\),
deletes at most \(\lfloor C_s(K)/(T+1)\rfloor\) high records, and uses only
the genuine cores of the remaining raw-low records.

A heavy raw-low core has two exact outcomes:

1. proper pair-difference span, producing a source-bound lower-rank child;
2. full span, producing a whole-family core and lossless shortening.

The second outcome cannot persist to \(K=s\).

## Rank-eleven payment

The uniform weighted projective-line theorem gives

\[
 |Z_1|\le4{,}070{,}947
\]

for every residual dimension.  Fixed cutoffs

\[
 (T_2,\ldots,T_{10})=(515,511,507,503,499,496,492,489,485)
\]

and complete scans of every \(s\le K\le1{,}048{,}576\) yield the top
requirement

\[
 L_{10}=248{,}706{,}399{,}341{,}288{,}370.
\]

The unsafe post-near load exceeds this by
26,274,328,769,971,774, so affine error rank eleven is paid, conditional on
the separately pinned near-rational deletion.

## Rank-twelve wall

At \(s=11,K=R\), scanning all 67,472 legal raw cutoffs gives the unique
minimum requirement

\[
 546{,}519{,}697{,}764{,}383{,}119
\]

at \(T=d\).  This is 271,538,969,653,122,975 above the available load.  The
single-threshold heavy-core method therefore cannot begin the rank-twelve
descent.

## Next theorem

The next maximal attack must couple multiple raw-margin levels, exploit
cross-pair/core correlations, or construct a chronology-correct owner.  The
withdrawn rank-two descendant and three-deficiency histogram are regression
controls only.
