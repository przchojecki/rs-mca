# Final hostile mathematics review

**Final mathematics verdict: GREEN.**

The repaired working tree no longer uses the false truncated-margin-to-raw
implication and no longer claims a rank-twelve payment or descendant. I
found no mathematical break in the raw-low shortening theorem, the uniform
rank-one cap, the all-dimension rank-eleven induction, the guarded dense-core
bound, or the stated rank-twelve single-threshold method wall.

This verdict is deliberately narrow:

- affine error rank eleven is paid **conditionally on** the separately pinned
  support-wise near-rational charge `|N|<=2w=134944`;
- affine error rank twelve remains unpaid;
- no active-v4 chronology or ledger atom is moved; and
- KoalaBear and the universal prize problem remain open.

The final statement-domain repairs were present in the bytes reviewed:
`1<=s<=K` is explicit in both the raw-low and dense-core theorems, and the
rank-one proof deletes only zero-normal coordinates whose incidence equation
is identically satisfied on the entire affine parameter line.

## Statement and source audited

I audited the current working-tree proofs of:

- `thm:mca-raw-low-heavy-core-shortening`;
- `thm:mca-uniform-rank-one-weighted-line`;
- `cor:mca-rank-eleven-repaired`;
- `thm:mca-dense-core-pair-type`; and
- `prop:mca-rank-twelve-single-threshold-wall`

in `experimental/grande_finale.tex`, together with the campaign proof,
primary verifier, independent exact auditor, result payload, claim/dependency
registries, the GF(11) truncated-margin regression, and the pinned #1160
near-rational proof artifact at
`c5f4ea7a0c78828c901ae5f3428894a8b2e2806b`.

The proof uses exact fixed KoalaBear parameters

\[
R=1048576,\qquad d=67472,\qquad 1\le K\le R.
\]

There is no asymptotic limiting step, layer cake, dyadic summation,
moment estimate, Markov inequality, or Chebyshev inequality in the audited
chain.

## 1. Raw-low incidence and shortening

**Status: PASS.**

For `1<=T<=d`, the resource controls

\[
\theta_\gamma=\min\{d+1,\widetilde\theta_\gamma\}.
\]

Every raw-high record has `theta_gamma>=T+1`; hence at most
`floor(C_s(K)/(T+1))` records are raw-high. On a raw-low selected support,
the minimizing pair has at least `m-T=d+K-T` genuine common-core
coordinates. The displayed heavy-coordinate load is therefore an honest
double count. The GF(11) control, where raw margin is `4` but truncated
margin is `3`, is rejected by this partition rather than being incorrectly
treated as a core of size `m-3`.

At a heavy coordinate, the pair-difference space

\[
U_x=\operatorname{span}\{a_\gamma-a_0,b_\gamma-b_0:x\in H_\gamma\}
\]

is a subspace of `C'`, and every generator vanishes at `x`.

- If `U_x<C'`, subtracting the incident base pair and dividing by the
  locator `X-x` puts the incident scalar explanations in a direction space
  of dimension at most `s-1`.
- If `U_x=C'`, every word in `C'` vanishes at `x`. Since all selected
  pair differences lie in `C'`, the same coordinate is in the pair core of
  every selected slope, giving a lossless whole-family shortening.
- At `K=s`, full span is impossible because
  `C'=F[X]_{<s}` contains the constant polynomial `1`.

The lift is support-correct. The complete scalar agreement domain
`A_gamma` contains the retained support and is itself pair-noncontained. A
pair explaining the shortened received pair on `A_gamma\{x}` would lift by
multiplication by `X-x` and addition of the base pair, contradicting original
pair noncontainment. If every `(m-1)`-subset of the shortened complete
domain were pair-contained, adjacent subsets overlap in at least `K-1`
coordinates; Reed--Solomon uniqueness and connectedness of the Johnson graph
would glue one pair on the whole domain. Thus an exact `(m-1)`
pair-noncontained child support exists.

### Post-near status

Post-near status is not needed by the raw-low incidence theorem, the
rank-one endpoint, or the induction after the initial family has been
selected. Nevertheless it is preserved. A shortened scalar codeword at
distance at most `d` lifts by multiplying by `X-x` and adding the scalar base
explanation; it agrees also at `x`, producing an original codeword at
distance at most `d`. This contradicts membership in the original
post-near complement. Exact-support reselection does not affect this
word-level property.

## 2. Uniform direction-rank-one cap

**Status: PASS.**

The proof now explicitly removes zero-normal coordinates identically
incident on the full affine parameter line; each is a root of the nonzero
direction polynomial, so their number is at most `K-1`. Nonroot coordinates
become affine graph lines and nonuniversal roots become vertical projective
lines. No root fiber is discarded.

For the low class, every exact support owns at least
`q(m'-q)` unordered cross-class coordinate pairs. Two distinct projective
lines meet in at most one parameter point, so these charges are disjoint.
For the high class, the dominant line is unique on the chosen exact support.
The remaining-weight relaxation is an upper bound, and the one-variable
convex/increasing calculation reduces the maximum to deficiencies
`a_i in {1,q}` without losing a feasible maximizer.

The two exact implementations independently scan every
`1<=j<=1048576`. Both give the maximum at `j=1`:

\[
W_{\rm low}=483,\qquad W_{\rm high}=4070464,
\qquad W=4070947.
\]

The rank-zero case is separately bounded by `n_K<4070947`.

## 3. Rank-eleven recurrence and induction

**Status: PASS, conditional on the pinned near deletion.**

The inverse-pigeonhole threshold is exact:

\[
\Lambda_s(K;M,T)=
\left\lfloor\frac{C_s(K)}{T+1}\right\rfloor+
\left\lfloor\frac{(M-1)(R+K)}{d+K-T}\right\rfloor+1.
\]

A parent family of this size forces either an `M`-slope lower-direction
child or a lossless shortening. The all-dimension scans prove each printed
recurrence row for every integer `s<=K<=R`. A lossless chain cannot pass
`K=s`, where full span is impossible, so it eventually gives the proper
lower-rank child. Induction terminates at the uniform rank-one theorem.

The exact terminal numbers are

\[
L_{10}=248706399341288370,
\]

\[
B_*-134944+1=274980728111260144,
\]

with certified difference

\[
26274328769971774>0.
\]

The #1160 two-anchor theorem was inspected at its exact pin. Under
`3d<=R`, it proves the imported `2d=134944` charge in distinct finite-slope
units. The corollary correctly presents this as an external conditional
dependency; it does not silently turn the charge into an active-v4 owner.

## 4. Guarded dense-core theorem

**Status: PASS.**

The theorem is now restricted to raw margins, `1<=T<=d`, `1<=s<=K`, and
the positive-denominator regime `h^2>(K-1)n`. Distinct selected pair cores
intersect in at most `K-1` coordinates. With `S=sum_x r_x>=th`, Cauchy gives

\[
S^2\le n\bigl(S+(K-1)t(t-1)\bigr).
\]

The split at `t=ceil(n/(2h))` is exhaustive. In the large branch,
`z^2/n-z` is increasing for `z>=n/2`, and solving the resulting inequality
gives the printed second term of `Q(K,T)`.

For a fixed pair, pair noncontainment supplies at least
`delta=max(1,m-|H_e|)` outside-core coordinates per owning slope. These
exception sets are disjoint because one outside coordinate determines at
most one finite slope. Hence the uniform fixed-pair multiplicity
`R-d+1=981105` is valid. Adding it to the disjoint raw-high resource gives
the printed bound. The small exhaustive control checked `2,234,476`
uniform-block families.

## 5. Rank-twelve boundary

**Status: PASS as a method wall only.**

The current source makes no rank-twelve payment, descendant, endpoint, or
capacity claim. Scanning all `67,472` legal single cutoffs at `s=11,K=R`
gives the unique minimum

\[
546519697764383119
\]

at `T=d=67472`. The adjacent cutoff `T=67471` gives
`546519951206547786`. The minimum exceeds the available unsafe load by

\[
271538969653122975.
\]

This proves only that the repaired single-threshold mechanism cannot force
its first rank-ten child at rank twelve. It is not a lower bound on the true
rank-twelve family.

## Exact replay

Commands rerun against the reviewed working tree:

- `python3 -B experimental/scripts/verify_kb_mca_rank11_repair_rank12_route_v1.py`
  — **PASS**, rank-one cap `4070947`, rank-eleven slack
  `26274328769971774`, rank-twelve shortfall `271538969653122975`,
  `2,234,476` finite controls;
- the same verifier with `--tamper-selftest` — **PASS, 8/8** mutations
  rejected;
- `python3 -B experimental/scripts/audit_kb_mca_rank11_repair_rank12_route_v1.py`
  — **PASS**, independent all-dimension replay;
- Sage GF(11) truncated-margin regression — **PASS** with
  `post_near_distance=3`, `raw=4`, `truncated=3`, `core=2`;
- direct exact replay of the two rank-twelve endpoint cutoffs — **PASS**;
- targeted `git diff --check` on the theorem and proof files — **PASS**.

Certificate and manifest sealing are assigned to a separate independent
publication gate.  This mathematics verdict neither certifies nor depends on
the state of that later gate; subsequent creation, verification, or revision
of the release manifest does not alter the mathematical conclusions above.

## Claim promotion

| claim | mathematics decision | boundary |
|---|---|---|
| `C-001` | **PROMOTE** | uniform rank-one cap `4070947` |
| `C-002` | **PROMOTE** | raw-low shortening for `1<=s<=K`, `1<=T<=d` |
| `C-003` | **PROMOTE CONDITIONALLY** | rank eleven paid assuming pinned #1160 near deletion |
| `C-004` | **PROMOTE** | guarded dense-core theorem only |
| `C-005` | **PROMOTE** | rank-twelve single-threshold method wall only |
| `C-006` | **REMAINS BLOCKED** | rank twelve is not paid |
| `C-007` | **REMAINS ZERO/KNOWN** | no active-v4 ledger movement |
| `C-008` | **REMAINS BLOCKED** | KoalaBear is not closed |

## Dependencies not independently regenerated

I did not regenerate the full foundational proof of the nonuniform
support-margin resource (`D-001`) from first principles, nor the entire
active-v4 chronology. I checked their exact interfaces used here. The
#1160 near-rational theorem was read and checked at its exact commit, but its
standalone release suite was not transplanted into this worktree. These are
declared dependencies/boundaries, not hidden assumptions in the new
shortening algebra.

**Final status: GREEN on the mathematics axis.**
