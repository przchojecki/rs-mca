# Certificate and custody audit

> **SUPERSEDED PRE-REPAIR REVIEW.** The arithmetic checked here includes
> withdrawn rank-twelve descendant data.  It is retained as regression
> provenance and is not a release attestation for the raw-low packet.

Verdict: **GREEN**.

The certificate binds the exact #1173 parent, the superseded unsubmitted
candidate, all row constants, the complete uniform rank-one scan, the corrected
rank-eleven descent table, the rank-twelve barrier table, the rank-two endpoint
wall, and all nonclaims.

The primary verifier reconstructs the result from constants and rejects
mutations of the uniform cap, one barrier load, the `279,911` excess, the
rank-eleven payment flag, and any rank-twelve/KoalaBear promotion.  The
independent audit uses separate product formulas.  The manifest hashes every
shipped artifact and the canonical result payload.
