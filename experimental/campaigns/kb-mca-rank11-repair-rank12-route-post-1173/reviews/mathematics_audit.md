# Adversarial mathematics audit

> **SUPERSEDED PRE-REPAIR REVIEW.** This audit covers the withdrawn
> truncated-margin recurrence and is retained only as failure provenance.
> It is not evidence for the raw-low theorem now in the active source.

Verdict: **GREEN for the corrected rank-eleven payment and the stated
rank-twelve route cut; RED for rank-twelve payment, KoalaBear closure, or
active-v4 ledger movement.**

## Defect found and repaired

The superseded unsubmitted draft compared every lower-rank descendant to the
final `K=1` rank-one endpoint.  That was not justified when the rank drop
occurred at larger ambient dimension.  A rank-one family may first drop to a
rank-zero parallel star.

The repair does not assert delayed descent.  It proves a rank-one cap uniform
over every shortened dimension by normalizing the one-dimensional direction
polynomial and treating all coordinate equations as weighted projective lines.
Universal coordinates are deleted; nonuniversal zeros remain as vertical
lines.

## Weighted-line theorem

The low branch charges cross-line coordinate pairs.  The high branch has one
unique dominant line, and all non-dominant weights consumed along a fixed
dominant line are disjoint.  The coordinatewise convex/increasing argument
legitimately reduces every effective deficiency to `{1,q}`.  The all-dimension
scan is exact and attains its maximum at residual dimension one.

## Corrected induction

The induction hypothesis is now uniform in ambient `K`.  A proper heavy-core
span produces a lower-rank family above the corresponding load threshold,
regardless of where the drop occurs.  A full span gives a whole-family
shortening coordinate.  Full span cannot persist at `K=s`.  This closes the
history gap.

## Dense-core theorem

For distinct pair types, complete cores intersect in at most `K-1`
coordinates.  The Cauchy bound is applied only after separating the
`th<n/2` case; the denominator is checked positive at every declared barrier.
The per-pair capacity uses disjoint outside-core exception sets and is
maximized at effective deficiency one.

## Rank-twelve boundary

The barrier schedule is strictly nested, so a proper drop above one barrier
enters the next rank above its barrier.  The all-dimension scan verifies every
load transfer.  The final `8,681,730`-slope descendant is exact.

The `K=2` capacity calculation proves only a route cut.  It does not prove that
the fifteen-type capacity relaxation is realizable, nor that it is impossible.
The remaining `279,912` saving must use compatibility between different pair
types.

## Nonclaims

No source chronology, v4 owner, rank-twelve payment, or row closure is inferred.
