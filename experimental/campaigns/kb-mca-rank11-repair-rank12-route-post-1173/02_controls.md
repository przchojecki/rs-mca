# Controls

| control_id | object | expected outcome |
|---|---|---|
| C-001 | full residual-dimension rank-one scan | no cap exceeds 4,070,947 |
| C-002 | raw-low rank-eleven scans | every fixed-cutoff parent requirement is at most the printed \(L_s\) |
| C-003 | truncated-margin falsifier | the GF(11) post-near record has raw 4, truncated 3, and core 2 |
| C-004 | rank-twelve method wall | all 67,472 cutoffs fail; the unique best cutoff is \(T=d\) |
| C-005 | dense-core guard | the theorem is used only with raw margin and \(T\le d\) |

## Positive controls

1. Complete scalar agreement domains are used for every locator lift.
2. Minimizing directions and exact supports are selected before each cutoff.
3. Raw-low records are separated before any core-size lower bound.
4. Proper pair-difference span lowers direction dimension.
5. Full span makes the heavy coordinate a core for the complete family.
6. Nonuniversal roots in the rank-one theorem remain vertical projective
   lines.
7. The full deployed ambient range is scanned at every rank.
8. No nested descendant loads are summed.

## Hostile controls

1. The old truncated-margin recurrence is rejected by an actual post-near
   GF(11) record.
2. The old forced load 5,201,865 and slack 1,130,918 are not retained.
3. The old rank-twelve descendant 8,681,730 is withdrawn.
4. The value 279,911 is not presented as a global capacity wall.
5. No active-v4 owner or ledger movement is inferred.
6. Rank twelve and KoalaBear remain open.

## Finite controls

The primary verifier scans all 1,048,576 residual rank-one rows, every
ambient row in each of the nine raw-low rank-eleven transitions, all 67,472
initial rank-twelve cutoffs, the weighted-line endpoint cells, and small
set-system controls for the guarded dense-core inequality.  A separate
implementation replays the arithmetic with independent product formulas.
