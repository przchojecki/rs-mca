# Adversarial mathematics audit

Verdict: **GREEN for the stated transverse payment and emitted terminal**;
**RED for any claim of rank-eleven or KoalaBear closure**.

## Quantifier checks

- The numerator universe is the actual anchor-good set `G_0`, not the complete
  anchor core.  This repairs the most dangerous possible overreach because a
  complete pair core can exceed `m`.
- Every represented pair type meets `G_0` in at least `2A-n` coordinates by a
  literal two-set inequality.
- The row-space partition is unique relative to the fixed anchor.  Summed group
  charges are therefore disjoint in pair-type and slope units.
- Actual explanation dimension may be below ten.  The falling-factorial
  quotient is monotone in annihilator dimension over the deployed range, so
  the printed rank-one/rank-two exponents nine/eight are safe maxima.

## Ordered-basis lemma

At each greedy step, the current span is a proper subspace of the annihilator.
Transversality bounds the number of labeled overlap columns in that span by
`h`; hence at least `c-h` unchosen labels extend the independent tuple.  An
ordered basis determines exactly one annihilator and one row space.  Clones and
zero columns are counted with multiplicity and trigger the nontransverse
branch when concentrated.

## Group charges

- Rank-one groups really have a fixed right factor and use the exact parent ray
  theorem.
- Rank-two groups lie in two affine translates of one two-dimensional RS
  direction space.  The dimension-two ordinary list cap is `252`, and its
  square is far below the actual sextic field size.
- The per-pair slope multiplier is `n-A`, obtained from the same fixed-pair
  outside-core injection used by the parent stack.

## Terminal checks

If transversality fails, `F<U^perp` is proper.  Therefore `W=F^perp` strictly
contains `U`.  Every evaluation column in `F` is annihilated by every
polynomial coefficient vector in `W`; the squarefree locator of the actual
distinct coordinates divides all members of `W`.

## Nonclaims enforced

The packet does not assert that all residual pair types lie in the emitted
`W`, that different emitted factors synchronize, or that the terminal already
has a first-match owner.
